# 视频运营看板：运营需求分析与优化方案

> 文档目的：从运营同事的使用场景出发，评估当前看板每个 Tab 的价值、缺口和优化方向。
>
> 本文是产品与数据口径方案，当前不包含前端代码修改。

## 一、结论先行

当前看板已经覆盖了用户、内容、剧种、搜索、首页入口和资源位等主要数据，但整体仍偏向“数据展示 + 变化提示”。运营真正需要的是：

> 当前业务是什么格局？最近哪里发生了变化？变化影响了什么？今天应该优先处理或放大什么？

因此，最终形态不应是把更多表格堆到页面上，而应让每个 Tab 都具备四层信息：

1. **当前状态**：规模、排名、占比、集中度。
2. **相对变化**：昨日、上周或选择区间的变化，以及净增量。
3. **归因拆解**：剧种、题材、标签、频道、客户端、内容等维度。
4. **运营判断**：风险、机会或观察项；明确证据边界和下一步动作。

当前最高优先级不是继续增加零散榜单，而是统一日期、数据口径、实体去重和事件筛选，让 Tab1 能从所有 Tab 中选出真正值得关注的少量事件。

## 二、运营使用原则

### 1. 看板不是“变化最大榜”，而是“业务影响优先榜”

一个指标变化很大，不等于值得进入今日关注。事件至少要满足：

- 影响用户规模、有效播放、消费深度、内容分发或入口效率之一；
- 有两个或以上相互支持的证据，或有明确的规模证据；
- 运营存在可执行的关注、调整、放大或暂缓动作；
- 样本量足够，不能被低基数或单个资源位波动占据名额。

正向增长但没有动作价值的普通变化，不应进入 Tab1。低曝光 Banner、孤立热搜词、新入榜但没有播放增长证据的内容，都更适合放在明细或观察区。

### 2. 所有模块同时看绝对值和相对值

绝对视角回答“现在谁最大、占比多少”；相对视角回答“谁正在变热、变冷或贡献了变化”。例如：热播榜既要有当前播放 Top30，也要有较上周净增量和周环比；剧种既要有播放份额，也要有份额变化和对全站增长的贡献。

### 3. 颜色表达业务含义，不只表达数学方向

- 风险：红色；机会：绿色；观察：黄色或中性。
- “上升”不一定代表好，“下降”不一定代表坏，资源成本、转化效率和业务目标要一起判断。
- 不用颜色作为唯一信息，仍应显示数字、箭头或文字状态。

### 4. 不超过证据范围

“同时下降”可以由数据直接支持；“导致”“因为内容不好”“应该扩大供给”需要更强的因果证据。没有同口径下游数据时，应明确写“暂无法确认是否传导至播放消费”。

## 三、Tab1「今日关注」

### 当前价值

Tab1 已经具备从核心数据、频道、板块、热搜、内容榜单和 Banner 中生成事件的能力，并展示标题、关键变化、关键依据、判断逻辑、今日关注和跳转入口。这是正确的产品方向。

### 需要修改

1. **事件按业务问题合并，而不是按指标拆分**。M站、安卓和 iOS 都是设备 DAU 下降时，应合并为“多端用户规模变化”，证据中再列端别；同一个入口的点击、内容点击和尝试播放也只形成一个事件。
2. **TOP0-5，不强行凑三条**。当天只有一或两条达到门槛时，就展示一或两条。
3. **风险与机会分开标记**。风险优先于机会，机会优先于普通观察；低价值变化不占位。
4. 每条事件固定输出：发生什么、关键证据、影响范围、判断逻辑、今日动作、置信度和数据日期。
5. 标题和正文必须显示当前数据日期及比较口径；不能出现数据已经更新但文案仍停留在 8 月 23 日的情况。
6. 跳转到其他 Tab 时，要携带事件日期、客户端、频道、剧种等筛选条件，避免“结论看的是一天、明细打开的是另一天”。
7. 增加“为什么入选”的折叠信息：业务影响、证据数量、规模、可行动性。默认不打扰快速浏览。

### 推荐事件优先级

1. 用户规模与消费质量同时恶化；
2. 有效播放或消费深度显著下降；
3. 入口下降并确认传导到内容点击/尝试播放；
4. 全站或重要剧种出现有规模的增长机会；
5. 内容、剧种、题材或标签的增长得到至少两类指标支持；
6. 搜索、Banner、猜你喜欢等单点变化，仅在能关联到播放或转化时升级为重点。

### 验收标准

运营打开 30 秒内能知道最重要的 1-5 件事；不出现重复事件、孤立低价值事件、过度因果文案或过期日期；每条都能点击进入同口径明细。

## 四、各 Tab 的建议

| Tab | 当前作用 | 建议新增或修改 | 优先级 |
|---|---|---|---|
| 核心数据 | 查看设备 DAU、新增设备、播放率、人均播放时长、人均播放次数及客户端差异 | 增加全站汇总、客户端贡献拆解、当前值/对比值/净变化；统一指标单位和日期；明确“有效播放”的定义；支持单日和区间 | P0 |
| 热播榜单 | 查看当前播放量排名 | 保留当前播放 Top30；新增较上周播放上升 Top20，字段包含当前播放 VV、上周 VV、净增量、周环比、排名变化、是否进入当前热播榜；按 `season_id` 去重；增加剧种/题材/标签分布及 Top 集中度 | P1 |
| 热搜榜单 | 查看搜索 VV、UV、排名和内容属性 | 明确 `search_cnt` 为搜索 VV、`search_uv` 为搜索 UV、按搜索 VV 降序；主表只展示可确认的剧名，原始热搜词保留在明细；过滤“我”“爱”等明显普通词和 `--`；增加单日昨日环比、区间周环比；增加热搜增长 Top20；映射失败进入待确认清单 | P0 |
| BL 内容消费 | 查看同性题材内容消费、Top 内容和 BL 占比 | 明确 BL 规则为 `plot_type` 包含“同性”；展示 BL 播放 VV、全站播放 VV 占比、BL 内部内容占比和趋势；区分内容播放 UV 求和与去重用户 UV；泰国+同性+普通话到国产剧的映射只影响展示剧种，不得改变 BL 归属；使用 Excel 已核验 ID 库并展示版本 | P0 |
| 剧种内容贡献 | 查看剧种播放占比和选定剧种 Top30 | 同时展示播放 VV、全站占比、占比变化百分点、净增量；增加剧种对全站增长的贡献排行；明确原始剧种和映射后展示剧种，不要把官方剧种汇总与重分类明细无提示混算；重点核查泰剧/国产剧占比 | P0 |
| 播放增长榜 | 查看内容播放变化和增长机会 | 从单内容增长榜升级为全站增长机会：内容增长 Top20 + 剧种/题材/标签/频道贡献；同时显示百分比增长和净增量；低基数单独标记；汇总“哪些维度贡献了全站净增长”，不要只看涨幅最大的内容 | P1 |
| 首页频道 | 查看频道点击、内容点击、尝试播放等漏斗 | 补齐频道点击 UV → 内容点击 UV → 尝试播放/详情播放 UV → 有效播放的漏斗和各步转化率；按有效播放规模和承接率识别最好/最差频道；显示较昨日、较上周和净变化；使用 `dramaConversion` 同口径数据 | P0 |
| 猜你喜欢 | 查看猜你喜欢入口及内容效果 | 明确使用 Quick BI 对应组件的曝光、点击、播放字段；展示曝光→点击→尝试播放/有效播放；增加内容、剧种和标签的增长贡献；低样本提示；日期和客户端筛选必须与源数据一致，缺数时显示“无数据”而不是沿用旧快照 | P1 |
| 首页板块 | 查看板块曝光、点击、CTR | 保留曝光 UV、点击 UV、CTR；增加下游播放/有效播放承接（有同口径字段时）；以业务影响而非 CTR 单独排序；曝光 UV<1000 的板块过滤或标记观察；支持板块、频道、客户端和日期筛选 | P1 |
| Banner 点击 | 查看 Banner 曝光、点击和点击率 | 继续执行“去重 + 曝光 UV≥1000”；展示位置、标题、曝光 UV、点击 UV、CTR；环比按列文字排序，不使用误导性的上下筛选箭头；有下游字段时补充播放/有效播放贡献；低基数不进入 Tab1 | P1 |
| 弹窗数据 | 查看弹窗资源位效果 | 增加曝光→点击→播放/有效播放链路，区分资源位表现和业务结果；按有效播放贡献排序；显示样本量和低基数提示；资源位异常只有在影响核心指标时才进入 Tab1 | P2 |
| 搜索漏斗 | 查看搜索进入、完成、点击和播放承接 | 使用同一个 Quick BI 搜索组件的字段，展示搜索页 UV、搜索完成 UV/率、内容点击 UV、尝试播放/首帧/5 分钟等链路；同时看绝对量和转化率；不要用不同接口的相近字段替代 | P1 |

## 五、热播、热搜和增长机会的重点补齐

### 1. 热播榜单

页面应同时有两个视角：

- **当前热播 Top30**：当前播放 VV、播放 UV、排名、占比、剧种、题材、标签。
- **较上周上升 Top20**：当前 VV、上周 VV、净增量、周环比、排名变化、是否当前热播榜、剧种/题材/标签。

运营最关心的不只是“谁第一”，而是“谁正在变热、增长来自哪一类内容”。增长排行应设置最低当前播放量或最低净增量门槛，避免小基数造成虚高百分比。

### 2. 热搜榜单

热搜词和剧名不是天然等价。建议数据链路为：

`接口热搜项 → 标准化热搜词 → 映射库 → 统一剧名 → 按搜索 VV 聚合和排序`

映射库应至少包含：`搜索词`、`标准化搜索词`、`剧名`、`来源文件`、`生效日期`、`匹配状态`、`备注`。无法映射的词不能伪装成剧名；主榜可以只展示剧名，但原始词应保留在可展开明细中，便于审计。

如果多个热搜词映射到同一剧名，应按剧名聚合搜索 VV/UV，并保留词级明细；如果同一个词对应多个剧，必须标记歧义，不应静默随机选择。单日榜按当天搜索 VV 降序；区间榜应按区间内搜索 VV 求和，或明确使用日均，不能把单日排名直接拼成周期榜。

### 3. 增长机会分析

建议把“播放增长榜”升级为运营机会模块，而不是再增加孤立榜单：

1. 全站播放总盘：当前 VV、对比 VV、净增量、环比。
2. 增长内容 Top20：内容、播放变化、净增量、剧种、题材、标签、是否入热播榜。
3. 剧种贡献：各剧种当前 VV、占比、净增量、对全站净增长贡献。
4. 题材贡献：各题材当前 VV、占比、净增量、增长贡献。
5. 标签贡献：各标签当前 VV、占比、净增量、增长贡献。
6. 热搜增长 Top3/Top20：热搜增长与播放增长交叉核查，只有两者一致时才升级为机会事件。

这样才能回答运营真正的问题：

> 最近全站增长最猛的是什么？它属于哪个剧种、题材和标签？增长是否已经形成规模？是否值得增加曝光或供给？

## 六、必须统一的数据与技术规则

### 1. 日期单一来源

所有 Tab 统一由实际可用数据计算默认日期、区间和比较日期。页面不能继续保留静态的 7 月 6 日—8 月 4 日等旧文案，而数据已经更新到 8 月 31 日或 9 月 6 日。页面应同时显示：数据日期、比较日期、覆盖范围、筛选条件。

### 2. 官方汇总与明细重算分开

官方接口返回的剧种汇总、搜索汇总和 Quick BI 页面组件数据，不应和内容明细重算结果无提示地混在一起。每个指标应记录：数据源、接口/组件、原始字段、转换规则、日期和筛选口径。

### 3. 统一实体键和去重

- 内容：优先使用 `season_id`，展示名只是显示字段。
- 热搜：标准化热搜词作为词级键，映射后的剧名作为展示/聚合键；有内容 ID 时优先使用 ID。
- 频道/板块：使用频道编码或稳定名称，不用页面显示文案作为唯一键。
- Banner/弹窗：位置 + 资源位 ID + 内容 ID，先去重再聚合。

### 4. 统一指标语义

在字段字典中固定：VV、UV、CTR、播放率、尝试播放、首帧、5 分钟、有效播放的定义和单位。特别要标注“内容 UV 求和不是全站去重用户 UV”。

### 5. 数据质量状态

所有模块需要区分：已取到、无数据、字段缺失、映射失败、低样本、跨日不可比。不能用上一日快照静默填充当前日期。

## 七、落地优先级

### P0：先让看板可信

- 统一日期和比较口径，移除过期静态日期。
- Tab1 事件按业务问题去重，TOP0-5，过滤低价值和无动作事件。
- 核查官方汇总与重分类明细的混算问题。
- 固定 BL、剧种、热搜的映射规则和字段字典。
- 每个 Tab 显示实际数据日期、筛选条件和数据状态。

### P1：让运营能发现机会

- 热播较上周上升 Top20。
- 全站剧种/题材/标签增长贡献。
- 热搜增长排行与播放增长交叉核查。
- 首页频道完整有效播放漏斗。
- 猜你喜欢、搜索漏斗补齐同口径下游指标。

### P2：提高效率和可解释性

- Tab1 的“为什么入选”折叠详情。
- 事件到明细的筛选联动和状态回传。
- 统一导出字段、保存筛选视图、运营备注。
- 数据质量和映射失败清单的管理页。

## 八、验收清单

- [ ] 任一 Tab 的默认日期等于该模块实际最新数据日期。
- [ ] 单日筛选只显示昨日环比；区间筛选只显示同长度区间或周环比，口径明确。
- [ ] 每个业务模块同时提供当前结构和相对变化。
- [ ] Tab1 不出现同一业务问题的重复卡片。
- [ ] Tab1 不用单指标、低基数、无动作价值的变化凑数。
- [ ] 每条重点事件都有至少两个关键证据或明确的规模证据。
- [ ] 每条重点事件都显示风险/机会/观察状态和数据边界。
- [ ] 热播、热搜、BL、剧种的内容实体能够稳定去重。
- [ ] 热搜榜不把普通词直接当成剧名；映射失败可追溯。
- [ ] 泰剧、国产剧和 BL 的重分类规则只在明确的展示口径中生效，不改变原始数据归属。
- [ ] 频道、搜索、猜你喜欢、首页板块、Banner、弹窗的下游播放影响能够区分“已确认”和“暂无法确认”。

## 九、当前依据与口径说明

本方案基于当前项目页面、脚本和本地数据快照梳理，主要数据来源及字段如下：

- **剧种汇总**：`seasonTypePlayRatio`，原始字段包括 `日期`、`剧种`、`剧种编码`、`播放UV`、`播放VV`、`播放VV占比`。
- **内容播放**：`seasonPlayVV` / `season_play_daily`，原始字段包括 `season_id`、`title`、`season_type`、`season_classify`、`plot_type`、`producer_region`、`play_count`、`play_uv`。
- **热搜**：`seasonSearchAuthInfo`，原始字段包括 `search_cnt`、`search_uv`、`search_rank`、`series_name`、`season_id`、`drama_type`、`plot_type`、`season_type`。
- **频道漏斗**：`dramaConversion`，用于频道点击、内容点击和尝试播放等同口径漏斗字段。
- **搜索漏斗、猜你喜欢**：应继续使用对应 Quick BI 页面/组件的数据字段，避免用相近接口替代。
- **BL 映射**：基于项目内 `data/bl_genre_mapping.json` 的 Excel 核验 ID 库；当前规则为泰国 + 同性 + 普通话对应的展示剧种重分类，BL 归属仍以内容题材字段为准。

当前项目中部分模块仍存在文件名日期与实际数据覆盖期不完全一致的情况，因此正式验收时必须以数据内容中的实际日期和来源元数据为准，不能只看文件名。

## 十、一句话目标

> 让运营先看到全站业务格局，再看到真正的增长或风险来源，最后得到有证据边界的动作建议；而不是看到一堆变化后自己重新拼分析链路。
