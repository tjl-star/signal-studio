# 人人视频运营数据看板：GPT 上下文说明

> 用途：在提出、评估或实施看板新增需求前，先阅读本文。本文描述当前 `dashboard-v2` 的真实页面结构、交互、数据口径和已知限制，避免把“当前实现”误认为“计划设计”。

## 1. 项目定位

- 项目目录：`dashboard-v2/`
- 页面标题：视频运营数据平台
- 产品定位：人人视频运营数据分析看板
- 当前重点客户端：安卓新人人（数据中常见值为 `android_rrsp_xb`，应用 ID 为 `22`）
- 当前全局日期目标：2026-08-30
- 当前展示方式：纯静态前端 + `data/` 本地 JSON/CSV 快照
- 运行依赖：浏览器、Python 静态 HTTP 服务；不依赖前端构建、不依赖运行时接口 Token
- 主要入口：`index.html`
- 主要逻辑：`app.js`
- 主要图表库：`echarts.min.js`

## 2. 当前导航层级

左侧导航是“主 Tab + 可展开子 Tab”：

```text
运营总览
用户与内容
  ├─ 核心数据
  ├─ 热播榜单
  ├─ 热搜榜单
  └─ 剧种播放占比
首页运营
  ├─ 首页频道
  ├─ 猜你喜欢
  └─ 首页板块
搜索运营
资源位运营
  ├─ Banner点击
  └─ 弹窗数据
```

当前代码入口对应关系：

| 页面 | `data-page` / DOM | 内容 |
|---|---|---|
| 运营总览 | `insight` / `page-insight` | 今日重点事件、运营诊断卡片 |
| 核心数据 | `overview` / `page-overview` | DAU、新设备、播放率、消费深度 |
| 热播榜单 | `content` / `page-content` | 播放榜单、Top10 图表、Top30 明细 |
| 热搜榜单 | `search` / `page-content` 的子视图 | 热搜总榜明细 |
| 剧种播放占比 | `genre` / `page-genre` | 剧种占比环形图和明细 |
| 首页频道 | `home` / `page-home` 的流量入口子 Tab | 首页频道流量漏斗与频道排行 |
| 猜你喜欢 | `guess` / `page-guess` | Quick BI 推荐入口漏斗与指标趋势 |
| 首页板块 | `sections` / `page-sections` | 板块排行和明细 |
| 搜索运营 | `search-funnel` / `page-search-funnel` | 搜索整体漏斗 |
| Banner点击 | `banner` / `page-banner` | Banner 排行和明细 |
| 弹窗数据 | `popup` / `page-popup` | 效果总览、弹窗排行、明细数据 |

## 3. 各 Tab 当前内容和图形

### 3.1 运营总览

这是默认首页，重点不是展示所有数据，而是从已加载的模块中筛选运营值得关注的变化。

- 图形形态：事件流/诊断卡片，不是传统大盘图表
- 内容：核心业务变化、上升、下降、新入榜、异常等事件
- 交互：点击事件可跳转到对应 Tab，并带入日期、客户端、频道或内容参数
- 设计原则：低价值和证据不足的变化不占位；没有重点事件时显示空状态
- 数据限制：诊断依赖多个业务模块的共同日期；单个模块没有该日期时不能凭其他数据补造

### 3.2 用户与内容 > 核心数据

#### KPI 卡片

- 设备 DAU
- 新增设备
- 播放率
- 人均播放时长
- 人均播放次数

部分 KPI 带较前一日环比，颜色遵循运营习惯：上升红色、下降绿色。

#### 图表和表格

- 设备 DAU 趋势：折线图
- 新增设备趋势：折线图
- 播放率趋势：折线图
- 客户端拆分：表格，列为客户端、设备 DAU、播放率、人均播放时长、人均播放次数
- 用户消费深度分析：两个互斥子视图
  - 播放率：单折线图
  - 人均播放时长 + 人均播放次数：双轴折线图

#### 筛选

- 客户端：安卓、iOS、M站
- 趋势日期：开始日期、结束日期
- 全局周期：近 30 天、近 7 天（当前代码的近 30 天文案仍有历史“截至 8 月 27 日”的残留，需要新增需求时注意）

#### 图表颜色

- DAU：绿色
- 新设备：蓝色
- 播放率：绿色/橙色主题视具体区域
- 消费深度：时长橙色、次数蓝色虚线

### 3.3 用户与内容 > 热播榜单

- 主要图形：播放 VV Top10 横向条形图
- 主要指标：播放 UV、播放 VV、昨日环比
- 榜单：真实 Top30 原始记录
- 排名：默认按原始排名；明细中保留接口原始排名，不在前端重新计算
- 缺失值：接口未返回的播放 UV 保留 `--`
- 明细字段：日期、排名、内容名称、播放 UV、播放 VV、昨日环比、聚集类型、内容分类、题材标签、榜单状态、数据状态

#### 交互和排序

- 日期下拉：按数据文件中实际存在的日期选择
- 排序按钮：排名、播放 VV、昨日环比
- 同一排序按钮再次点击：切换升序/降序
- 默认排序：播放 VV 或原始排名，具体视当前明细视图状态
- 明细面板：默认收起，点击“查看明细”展开

### 3.4 用户与内容 > 热搜榜单

- 当前实现：复用内容榜单的明细区域，不再单独创建另一套搜索分析页面
- 内容：热搜总榜 Top30
- 字段：排名、搜索词/内容名称、搜索次数（或搜索 VV）、搜索 UV、昨日环比
- 默认：查看总榜，状态为全部，按排名升序
- 数据日期：当前实现取热搜文件中最新实际日期；若文件只有 8 月 29 日，不能显示真实的 8 月 30 日数据
- 重要限制：热搜不能用热播或其他模块的数据代替

### 3.5 用户与内容 > 剧种播放占比

- 主图形：环形饼图
- 图中信息：各剧种播放 VV 及占比
- 明细：剧种、播放 VV 占比、昨日环比、播放 VV
- 排序：按播放 VV 降序
- 筛选：独立日期控件，日期范围来自剧种数据文件
- 环比：当前日期与该剧种上一实际日期的播放 VV 占比比较
- 缺失日期：数据文件没有某日时，日期控件不会凭空生成该日数据

### 3.6 首页运营 > 首页频道

- 图形：频道入口排行横向条形图
- 链路展示：
  1. 首页频道点击 UV
  2. 内容点击 UV
  3. 详情播放 UV
  4. 播放超过 5 分钟 UV
  5. 有效播放 UV
- KPI：链路各阶段 UV
- 明细：频道、首页频道点击 UV、内容点击 UV、详情播放 UV
- 口径：按接口原始口径展示，不在页面虚构流失率
- 数据限制：当前首页流量数据快照日期较旧时，首页频道无法真实展示 8 月 30 日

### 3.7 首页运营 > 猜你喜欢

- 数据源：Quick BI / recommend_data 快照
- 默认客户端：全客户端（不是安卓新人人专属）
- 主图形：推荐入口漏斗
- 漏斗阶段：Tab 点击 UV、内容曝光 UV、内容点击 UV
- 质量指标：首帧播放整体转化率、播放转化率、5 分钟转化率、人均播放时长
- 趋势图：折线图，可切换指标
- 可切换指标：Tab 点击 UV、内容曝光 UV、内容点击 UV、内容点击率、首帧转化率、播放转化率、5 分钟转化率、人均播放时长
- 日期：独立日期控件；若 Quick BI 快照最新到 8 月 27 日，选择 8 月 30 日会回退到最新实际记录
- 重要限制：Quick BI 口径不能用 data_provider 的相近字段替代

### 3.8 首页运营 > 首页板块

- 主图形：板块效果排行横向条形图
- 指标选择：曝光 UV、点击 UV、点击率 UV 等，具体以当前页面下拉选项为准
- 明细：日期、频道、板块/子板块、曝光 UV、点击 UV、点击率 UV、较前一日变化、风险状态
- 筛选：排行日期、排行频道、排行指标；明细日期、明细频道
- 排序：点击表头按钮切换字段排序和方向
- 分页：页码、上一页、下一页、每页条数、跳转页码
- 异常：保留异常数据在明细中，但排行可排除点击 UV > 曝光 UV、点击率 > 100% 等异常
- 口径：安卓新人人视频，应用 ID 22，低风险；不能把全部客户端/全部应用数据混入

### 3.9 搜索运营 > 搜索漏斗

- 当前只有一个页面，不需要再增加搜索运营子 Tab
- 主图形：动态漏斗/分层梯形视觉
- 漏斗阶段：进入搜索页 UV、搜索完成 UV、影视点击 UV、详情页起播 UV、播放 5 分钟 UV
- 右侧指标：搜索完成率、首帧转化率、5 分钟转化率
- KPI 环比：各阶段与上一实际日期对比
- 筛选：数据日期
- 数据源：Quick BI 搜索整体数据
- 口径：层级 UV 使用接口原始值，相邻转化率按相邻阶段 UV 计算
- 限制：搜索整体数据不能用搜索热榜数据代替

### 3.10 资源位运营 > Banner 点击

#### 排行区

- 图形：Banner 横向条形图
- 排行切换：点击 UV TOP10、点击率 TOP10
- 筛选：客户端（安卓/M站）、日期、Banner 位置
- 条形图颜色：沿用 Banner 点击模块统一颜色

#### 明细区

- 字段：日期、Banner 位置、Banner 标题、曝光 UV、点击 UV、点击率、点击 UV 昨日环比、点击率昨日环比、榜单状态
- 默认过滤：曝光 UV ≥ 1,000
- 榜单状态筛选：全部、新入榜
- 排序：不设置独立排序下拉框；点击表头直接排序
- 可排序字段：曝光 UV、点击 UV、点击率、点击 UV 昨日环比、点击率昨日环比
- 排序方向：同一表头重复点击切换升序/降序
- 表头箭头：不需要固定显示无意义箭头，只在当前排序字段显示状态
- 分页：页码、上一页、下一页、每页条数、跳转

### 3.11 资源位运营 > 弹窗数据

- 固定客户端：安卓新人人
- 顶部日期：独立日期控件，当前目标上限为 2026-08-30
- 子 Tab：效果总览、弹窗排行、明细数据

#### 效果总览

- KPI：曝光 UV、点击 UV、跳转播放 UV、有效播放 UV、点击率、转化率、有效播放率
- 环比：KPI 显示数字 + 箭头；上升红色、下降绿色
- 图形：核心指标趋势或效果分布图，使用 ECharts 动画

#### 弹窗排行

- 图形：弹窗组件横向条形图
- 指标切换：播放 UV、点击 UV、曝光 UV 等当前实现提供的指标
- 阈值筛选：默认有效曝光阈值 1,000，可按页面控件调整
- 排序：按当前选择的排行指标降序

#### 明细数据

- 筛选：组件名称/组件 ID 搜索、日期
- 字段：组件名称、组件 ID、日期、曝光 PV、曝光 UV、点击 PV、点击 UV、跳转播放 PV、跳转播放 UV、有效播放 PV、有效播放 UV、点击率、转化率、有效播放率
- 排序：点击表头实现升序/降序，不使用排序下拉框
- 分页：页码、上一页、下一页、每页条数、跳转
- 异常：点击率、转化率或有效播放率超过 100% 的行保留并标记异常，不擅自修正原始数据
- 交互动画：点击图表后产生波浪展开效果，配合 ECharts 更新动画

## 4. 通用交互规范

### 日期

- 当前产品目标是统一到 2026-08-30。
- “展示日期”与“数据实际覆盖日期”必须区分。
- 不允许把 8 月 27 日、8 月 29 日或 8 月 4 日的数据改标为 8 月 30 日。
- 数据缺失时应显示“暂无 8 月 30 日真实数据”，或在数据补齐后再展示。
- 日期输入的最大值应来自数据实际覆盖或产品目标，不能通过前端文案制造数据。

### 环比颜色

- 上升：红色 + 上箭头
- 下降：绿色 + 下箭头
- 无法比较：`--`
- 只展示数字 + 箭头时，不额外增加冗长说明

### 表格排序

- 明细表优先采用点击表头排序，避免增加排序下拉框。
- 默认数值字段降序，文本/日期字段按升序。
- 再次点击同一表头切换方向。
- 当前排序字段才显示排序状态；未排序字段不固定显示箭头。
- 排序必须在当前筛选结果内进行，不能跨日期或跨客户端混排。

### 图表动画

- ECharts 图表默认启用进入动画和更新动画。
- 条形图点击后可使用波浪展开/反馈效果。
- `prefers-reduced-motion` 用户应关闭动画或降低动画强度。
- 动画只改变表现层，不改变数据、排序和口径。

## 5. 日期与数据覆盖现状（审计记录）

以下是当前本地快照检查结果，不代表所有模块已经补齐到 8 月 30 日：

| 模块/文件 | 当前可见最新日期 | 备注 |
|---|---:|---|
| 弹窗数据 | 2026-08-30 | 安卓新人人，目标口径 |
| 首页板块 | 2026-08-30 | 部分文件存在旧版数据 |
| 频道运营 | 2026-08-30 | 安卓新人人 |
| 热播榜单 | 2026-08-30 | 真实 Top30 |
| 核心 DAU | 2026-08-30 | 客户端拆分 |
| 人均播放时长/次数 | 2026-08-30 | 客户端拆分文件 |
| Banner 排行快照 | 2026-08-30 | 全客户端文件 |
| 热搜 | 2026-08-29 | 需要补齐 8 月 30 日 |
| 猜你喜欢 | 2026-08-27 | Quick BI 快照，需要独立补齐 |
| 首页流量漏斗 | 2026-08-04 | 需要更新对应快照 |
| Banner 明细 | 2026-08-04 | 与 Banner 排行快照不是同一文件 |

因此，新增“统一日期”需求时，先判断是：

1. 只统一控件和文案；还是
2. 重新查询/导出缺失日期的真实数据；还是
3. 两者都做。

## 6. 数据文件与更新脚本

### 前端主要数据映射

数据映射集中在 `dashboard-v2/app.js` 的 `DATA_PATHS`：

| 页面模块 | 前端数据文件/来源 |
|---|---|
| 核心 DAU | `data/new_people_video_daily_active.json` |
| 人均播放次数 | `data/avg_play_count_by_client.json` |
| 播放率 | `data/播放率_近30天_按客户端.json` |
| 人均播放时长 | `data/人均播放时长_近30天_按客户端.json` |
| 热播榜 | `data/站内播放排名Top30_20260706_20260804.json` |
| 热搜 | `data/热搜总榜_20260704_20260804.json` |
| 剧种 | `data/剧种播放占比_全部端口_20260705_20260804.json` |
| 首页频道 | `data/首页流量与转化漏斗_20260705_20260804.json` |
| 首页板块 | `data/home_section_ops_20260705_20260804.json` |
| Banner | `data/banner_click_20260704_20260804_all_clients.json`、`data/banner_click_detail.json` |
| 弹窗 | `data/popup_window_20260701_20260827_android_rrsp_xb.json` |
| 猜你喜欢 | `data/guess_you_like_home_quickbi_20260701_20260825.json` |
| 搜索整体 | `data/search_overall_conversion_20260701_20260825.json` |

### 更新脚本

当前项目有 `dashboard-v2/update_daily_data.py`，用途是按模块刷新、校验并输出覆盖报告。默认行为是预览，只有带 `--apply` 才写入数据。

主要模块路由：

| 更新模块 | 数据接口/来源 | 关键筛选 |
|---|---|---|
| `overviewCore` | `coreData` | 按客户端/平台 |
| `contentRanking` | `seasonPlayVV`、`playTop10` | 总榜、新用户榜 |
| `searchHot` | `seasonSearchAuthInfo` + `seasonPlayVV` 元数据 | 热搜榜 |
| `newSearchHot` | `seasonSearchAuthInfo` | 新用户热搜 |
| `genreRatio` | `seasonTypePlayRatio` | 全端口 |
| `depthClients` | `perCapitaWatchDuration`、`perCapitaPlayCount` | 按客户端 |
| `channelOps` | `dramaConversion` | `android_rrsp_xb`、按频道 |
| `sectionOps` | `sectionData` | `android_rrsp_xb`、应用 ID 22、低风险 |
| `bannerClick` | `bannerClickData` | 全客户端 |
| `popupWindow` | `popupWindowData` | `android_rrsp_xb` |
| `guessYouLikeQuickBI` | Quick BI MCP/OLAP 快照 | 不由 Python data_provider 替代 |
| `searchOverallQuickBI` | Quick BI MCP/OLAP 快照 | 不由 Python data_provider 替代 |

示例：

```powershell
python dashboard-v2\update_daily_data.py --start-date 2026-08-30 --end-date 2026-08-30
python dashboard-v2\update_daily_data.py --start-date 2026-08-30 --end-date 2026-08-30 --apply
```

更新任何数据前，应先确认数据源、原始字段、日期、客户端、应用和风险筛选，禁止使用 mock 值或相近字段替代目标数据。

## 7. 新需求评估规则

GPT 在评估新增需求时，按以下顺序判断：

1. 需求属于哪个主 Tab/子 Tab？是否会破坏现有导航层级？
2. 需求需要新增数据字段，还是只改展示层？
3. 数据是否已有真实快照？覆盖日期、客户端和应用口径是否一致？
4. 是否会与已有排序、筛选、分页、环比逻辑冲突？
5. 图形是否适合指标类型：趋势用折线、排行用横向条形、构成用饼图、链路用漏斗、逐条记录用表格？
6. 是否会增加重复信息、杂项卡片或大面积空白？
7. 是否需要同步更新 `app.js`、HTML、CSS、数据快照、README 和更新脚本？
8. 是否需要数据源查询或导出？如果需要，先核对数据源路由和字段口径，再实施前端改动。

## 8. 当前已知问题和注意事项

- 各模块数据快照日期不完全一致，不能仅修改日期文字解决。
- 部分 HTML 初始文案仍保留历史日期，实际运行时会被 JS 覆盖，但应在后续改动中清理。
- `app.js` 存在多层兼容/覆盖式渲染逻辑，修改函数时要搜索同名函数后续是否被重新赋值。
- 搜索整体和猜你喜欢属于 Quick BI 口径，不能从 data_provider 接口找相近字段替代。
- 弹窗数据和 Banner 明细包含异常值时，应保留原始值并标记，不得未经确认进行修正。
- 新仓库运行包是静态快照版本，公共仓库不应包含接口 Token、密码或本地绝对路径。
- 直接双击 `index.html` 可能触发浏览器跨域限制，建议通过 `python -m http.server 8000` 运行。

## 9. 变更交付检查清单

- [ ] 主 Tab/子 Tab 位置符合现有导航结构
- [ ] 页面内容没有重复卡片和无意义空白
- [ ] 日期控件、页面文案和实际数据日期一致
- [ ] 客户端/应用/风险筛选明确且没有混口径
- [ ] 排序在表头实现，方向和当前状态可见
- [ ] 环比颜色符合“上升红、下降绿”
- [ ] 图表颜色与同类现有模块一致
- [ ] 图表动画不改变数据逻辑
- [ ] 缺失数据显示空状态，不使用 mock 或旧日期冒充
- [ ] 本地 HTTP 服务可启动，页面资源和 JSON 可加载
- [ ] 如有数据更新，完成接口、字段、日期和行数校验
- [ ] 更新 README/文档，说明新增内容和运行方式
