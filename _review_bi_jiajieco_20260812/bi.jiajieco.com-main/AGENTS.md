# Jiajieco BI 项目协作约定

## 项目结构

- `frontend/`：Vue 3、Vite、Element Plus、ECharts。
- `backend/`：FastAPI、SQLAlchemy。
- 后端业务接口统一使用 `/api/v1` 前缀。
- ODS 数据库是业务分析数据源，管理账号等本地数据使用独立数据库。

## 本地运行

- 前端默认启动在 `http://127.0.0.1:5174`，不要改回 Vite 默认的 5173。
- 后端默认启动在 `http://127.0.0.1:8000`。
- 前端启动：`npm.cmd run dev -- --host 127.0.0.1 --port 5174`。
- 后端启动：在 `backend/` 下执行 `.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000`。
- 后台启动进程时使用隐藏窗口，并把标准输出和错误写入项目现有日志文件。
- 重启服务前必须确认端口上的进程确实属于本项目，避免终止其他程序。

## 页面与交互约定

- 延续现有 Element Plus 组件和看板设计语言，不另建一套风格。
- 销售品牌看板使用玫红色调；库存类看板使用克制的绿色调。
- 左侧导航必须清楚高亮当前页面。
- 库存分析导航使用“品牌周转”名称，不再显示“库存周转”。
- 筛选条件尽量同步到 URL，刷新后应保留当前页面和筛选状态。
- 表格默认支持点击列头排序；数据量较大时使用后端分页。
- 筛选框保持紧凑，不让日期控件自动占满整行；移动端再切换为整行宽度。
- 金额统一保留两位小数，数量一般不显示小数，百分比突出显示。
- 图表提示框中的浮点数必须格式化，不能出现长小数尾数。

## 业务口径

### 销售与渠道

- 渠道只向业务展示“线上”和“线下”两类，不使用含义不清的“销售渠道”分类。
- 配置了明确线上平台的渠道归为线上；未设置线上平台的渠道默认按线下处理。
- 品牌销售页面的总销售额应与线上、线下销售额合计保持一致。
- 正装和小样需要能够分别查看销售金额和销售数量。

### 库存与周转

- 库存相关分析优先使用“可用库存”，因为库存存在预留和锁定情况。
- 商品周转默认货品分类为“正装、小样”，默认库存不少于 100 件。
- 品牌周转默认不指定品牌，并默认展示上一个已经结束的完整季度。
- 周转天数越长表示周转越慢；库存过少时应通过库存分段筛选减少失真。
- 品牌周转中的“未归类”默认不进入品牌周转图表。
- ODS 库存数据来自每日同步快照，不是业务系统实时库存；页面需要展示或保留数据更新时间。

### 品牌月度到货

- 页面分为“到货看板”和“数据明细”两页。
- 默认时间为本年度截至当天，同时支持自定义起止日期。
- 支持品牌、货品分类、入库仓库多选筛选；入库仓库默认不选择，代表全部仓库。
- 自定义日期需要按实际入库日期精确统计，不能仅依赖月度汇总数据。
- 到货看板需要展示净到货数量、到货成本、入库单数、到货品牌数。
- 货品横向图展示货品名称、净到货数量和最近入库日期。
- 正装和小样分别展示到货数量，并提供按入库日期的趋势图。
- 数据明细提供“全部明细、正装明细、小样明细”，并展示入库单、品牌、货品、分类、仓库、供应商、数量和成本信息。

## 数据开发约定

- ODS 查询只读，不在分析接口中修改源表。
- 中文表名和字段名在 SQL 中使用反引号包裹。
- 汇总指标与明细必须使用相同的日期、品牌、货品分类和仓库条件。
- 需要精确到日或货品分类时，以入库单和入库明细为准；月度汇总用于月度核对和趋势汇总。
- 去重口径必须明确：入库单按 `docId` 去重，SKU 按货品编号去重。
- 红冲或负数记录应纳入净额、净数量计算，不能直接过滤掉。
- 新增筛选条件时，必须同步更新缓存键，防止不同条件返回同一份缓存数据。

## 修改与验证

- 保留用户已有改动，只修改当前需求涉及的文件。
- 后端修改后至少执行：`.venv\Scripts\python.exe -m py_compile app\api\routers\<文件>.py`。
- 前端修改后至少执行：`npm.cmd run build`。
- 涉及筛选或统计口径时，至少验证一次默认条件和一次组合筛选。
- 涉及视觉问题时，必须检查浏览器中的实际计算样式和页面渲染，不能只根据源代码判断。
- 不把账号、密码、访问令牌或设备登录码写入代码、文档和日志。

