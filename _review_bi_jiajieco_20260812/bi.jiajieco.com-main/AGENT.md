# 项目开发说明

本文件记录 `bi.jiajieco.com` 已确认的数据来源、指标口径和开发约定。

新增页面、修改数据表或调整统计口径后，需要同步更新本文件，避免不同页面使用不同的数据定义。

## 技术架构

- 前端：Vue 3、Vite、Vue Router、Pinia、Element Plus、ECharts
- 后端：FastAPI、SQLAlchemy
- 登录库：SQLite
- 业务源数据：MySQL `ods` 只读库
- 在线分析数据：独立 MySQL `ads` 库
- 后端目录：`backend`
- 前端目录：`frontend`

### 当前数据链路

```text
ODS 业务视图（只读）
  → 定时/按需 ADS 构建与口径对账
  → 带版本号的 ADS 汇总和窄表
  → FastAPI 在线只读查询
  → Vue 看板
```

- 生产环境使用 `BI_QUERY_SOURCE=ads`，事实指标优先从最新 `ready` ADS
  版本读取。
- 在线应用账号只拥有 ADS 的 `SELECT`、`SHOW VIEW` 权限；构建账号与在线
  查询账号分离。
- ODS 不承担高频在线聚合。渠道等体量很小、需要保持最新的维表可以实时读取，
  但事实统计仍必须来自 ADS。
- ADS 构建先生成新版本并完成核对，再把批次标记为 `ready`。在线接口不得读取
  `building` 或 `failed` 版本。
- ODS、ADS、后台管理库必须保持逻辑和权限隔离，分析接口不得写入 ODS。

## 本地启动

后端：

```powershell
cd D:\code\bi.jiajieco.com\backend
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload
```

前端：

```powershell
cd D:\code\bi.jiajieco.com\frontend
npm.cmd run dev -- --host 127.0.0.1 --port 5174
```

访问地址：

```text
前端：http://127.0.0.1:5174
后端：http://127.0.0.1:8000
```

本地账号由后台管理库单独维护，不在代码或文档中保存默认密码。

## 接口约定

后端接口统一返回：

```json
{
  "code": 0,
  "message": "ok",
  "data": {}
}
```

销售接口前缀：

```text
/api/v1/sales
```

相关文件：

```text
后端：backend/app/api/routers/sales.py
前端：frontend/src/api/sales.js
```

## 时间筛选约定

销售分析页面统一提供：

- 近30天：`last_30`
- 本月：`this_month`
- 自定义开始日期：`start_date`
- 自定义结束日期：`end_date`

交互规则：

- 用户选择日期时不立即查询数据库。
- 点击“查询”或刷新按钮后才请求接口。
- 自定义日期必须同时提供开始和结束日期。
- 结束日期包含当天全部数据，SQL 使用 `< DATE_ADD(:end_date, INTERVAL 1 DAY)`。

### 销售单查询口径页面的日期锚点

销售概览、销售明细、渠道分析的 ADS 构建日期范围，锚定：

```text
ods.销售单查询.下单时间 的最大日期
```

### 销售单明细账口径页面的日期锚点

商品排行、品牌分析、品牌渠道下钻的 ADS 构建日期范围，锚定：

```text
ods.销售单明细账.下单时间 的最大日期
```

这样可以避免明细账数据没有更新到最新日期时，页面默认打开空白范围。

## 销售分析接口缓存

后端销售分析接口使用进程内 TTL 缓存：

```text
缓存时间：300 秒
最大条目：256
缓存键：接口名称 + 查询参数
```

适用接口：

```text
/sales/overview
/sales/detail
/sales/product-rank
/sales/brand-analysis
/sales/channel-analysis
/sales/brand-channel-analysis
```

说明：

- 同一个接口、同一组查询参数，5 分钟内重复访问会直接返回缓存结果。
- 缓存只放在后端进程内；如果后续部署多进程、多实例，需要迁移到 Redis。
- 该缓存用于减少相同参数的重复 ADS 查询，不改变任何销售口径。

缓存不是性能优化的替代品：每个核心接口在未命中缓存时也必须满足性能目标。
如果生产扩展到多进程或多实例，进程内缓存需要迁移到 Redis，并保留请求合并
（singleflight）能力，避免同一缓存键并发击穿。

## ADS 查询与性能约定

- 列表、明细和筛选项优先使用面向页面的 ADS 窄表，不在请求链路扫描 ODS
  百万级事实表。
- 能在 SQL 中完成的 `GROUP BY`、Top N、分页和排序必须下推数据库，禁止把
  数千行日明细拉回 Python 后再聚合。
- ADS 索引应以 `data_version`、核心筛选维度、日期为前缀，并使用实际默认条件
  和组合筛选执行计划验证。
- 品牌销售下钻的商品排行与销售负责人正装/小样汇总在 ADS 中聚合后返回。
  不再回传全部“日期 × 渠道 × 商品”事实行给应用层处理。
- 新增筛选条件时，SQL 条件、汇总口径、明细口径和缓存键必须同时更新。
- 任何性能改造都要核对优化前后的订单数、净销售额、净数量及 Top N 结果；
  至少覆盖默认条件和一组组合筛选。
- 当前性能验收目标：
  - 常用 ADS 冷查询不超过 1 秒，复杂页面不超过 3 秒。
  - 热访问 P95 不超过 500 ms。
  - 页面不得依赖缓存才能避免超时。

后端响应通过 `Server-Timing` 以及 `X-ODS-*`、`X-ADS-*`、`X-DB-*`
诊断头提供查询次数与耗时。慢查询日志只记录 SQL 指纹，不记录参数、账号、
Token 或业务明细。

## 发布与 ADS 刷新

GitHub Actions 的 `Deploy BI` 默认只发布应用，不重建 ADS。普通代码变更应走
快速发布路径；只有数据模型、构建口径或 ADS 表内容需要刷新时才勾选：

```text
rebuild_sales_ads
rebuild_inventory_ads
```

需要重建 ADS 时，工作流先使用候选后端镜像构建并核对数据，成功后才切换应用
目录；构建失败或发布取消不得替换当前线上版本。部署使用串行并发组，避免两次
生产发布相互覆盖。

发布后必须完成：

1. 前端与 OpenAPI 健康检查。
2. 核心销售、库存和下钻接口的认证 ADS 健康检查。
3. 确认响应头为 `X-BI-Query-Mode=ads` 和
   `X-BI-Response-Source=ads`。
4. 涉及页面交互时，在生产浏览器验证实际渲染、筛选切换和控制台错误。

销售和库存 ADS 每日定时刷新，任务使用互斥锁避免重叠。应用发布与数据刷新是
两个独立动作，不能为了发布一行代码默认触发全量数据重建。

## 销售汇总统一口径

销售分析一级页面顶部汇总卡的源数据口径统一使用：

```text
ods.销售单查询
```

生产在线接口读取由该口径构建出的 ADS 事实表，不在页面请求中直接扫描此 ODS
视图。

指标定义：

```text
订单数：COUNT(DISTINCT CASE WHEN COALESCE(货品数量, 0) > 0 THEN 订单编号 END)
销售额：SUM(实付金额)
销售数量：SUM(货品数量)
时间字段：下单时间
客单价：销售额 / 订单数
有效订单：COALESCE(订单状态, '') NOT LIKE '%取消%'
```

说明：`销售单查询` 的订单数只统计正向销售订单；销售额和销售数量仍保留净值，因此会包含退货或冲销产生的负数。

使用统一汇总口径的页面：

- 销售概览
- 销售明细
- 商品销售排行
- 品牌销售分析
- 渠道分析

注意：`销售单查询` 和 `销售单明细账` 的数据粒度不同，不能直接混用顶部汇总指标。

曾确认的近30天统一汇总结果（旧口径，未过滤取消单，仅保留作历史排查参考）：

```text
开始日期：2026-06-08
结束日期：2026-07-07
订单数：415,899
销售数量：3,836,637
销售额：289,096,993.40
```

## 销售分析页面

### 销售概览

路由：

```text
/sales/overview
```

接口：

```text
GET /api/v1/sales/overview
```

数据来源：

```text
ods.销售单查询
```

使用字段：

```text
订单编号
下单时间
销售渠道
实付金额
货品数量
```

页面内容：

- 销售额、订单数、销售数量、客单价
- 销售趋势
- 渠道排行

### 销售明细

路由：

```text
/sales/detail
```

接口：

```text
GET /api/v1/sales/detail
```

数据来源：

```text
ods.销售单查询
```

使用字段：

```text
订单编号
订单状态
结算状态
销售渠道
下单时间
货品摘要
货品数量
应收合计
实付金额
市
```

页面规则：

- 明细使用分页查询。
- 关键词匹配订单编号或货品摘要。
- 订单数只统计 `货品数量 > 0` 的订单编号去重。

### 商品销售排行

路由：

```text
/sales/product-rank
```

接口：

```text
GET /api/v1/sales/product-rank
```

顶部汇总：

```text
ods.销售单查询
```

商品排行：

```text
ods.销售单明细账
```

排行字段和口径：

```text
时间字段：下单时间
商品：货品名称
订单数：COUNT(DISTINCT 订单编号)
销售数量：SUM(数量)
销售额：SUM(分摊后金额)
件均价：销售额 / 销售数量
销售占比：商品销售额 / 明细账当前筛选总销售额
```

重要：不要使用 `销售单查询.货品摘要` 做商品排行。货品摘要可能包含整张订单的多个商品，直接分组会产生异常长商品名称。

### 品牌销售分析

路由：

```text
/sales/brand-analysis
```

接口：

```text
GET /api/v1/sales/brand-analysis
```

顶部汇总：

```text
ods.销售单查询
```

品牌排行：

```text
ods.销售单明细账
```

排行字段和口径：

```text
时间字段：下单时间
品牌：优先使用品牌字段
订单数：COUNT(DISTINCT 订单编号)
销售数量：SUM(数量)
销售额：SUM(分摊后金额)
商品数：COUNT(DISTINCT 货品名称)
件均价：销售额 / 销售数量
```

部分数据的品牌字段为空，后端仅对已知品牌使用货品名称前缀兜底，其他归入“未识别品牌”。

### 品牌渠道下钻

路由：

```text
/sales/brand-analysis/:brand
```

接口：

```text
GET /api/v1/sales/brand-channel-analysis
```

必填参数：

```text
brand
```

数据来源：

```text
品牌销售事实：ods.销售单明细账
渠道维度：ods.渠道列表
关联条件：销售单明细账.销售渠道 = 渠道列表.渠道名称
时间字段：销售单明细账.下单时间
```

允许展示的指标：

```text
净销售额：SUM(分摊后金额)
明细行数：COUNT(*)
订单数：COUNT(DISTINCT 订单编号)
销售数量：SUM(数量)
客单价：净销售额 / 订单数
件均价：净销售额 / 销售数量
销售占比：分组净销售额 / 品牌总净销售额
```

当前不展示、不计算：

```text
毛利
毛利率
成本
```

页面内容：

- 品牌净销售额、订单数、销售数量、客单价、件均价
- 品牌销售趋势
- 渠道类型表现
- 线上平台表现
- 渠道名称表现
- 品牌商品排行

负数量和负金额是原始数据中的退货或冲销，不能直接删除。品牌下钻页面统一标记为“净销售额”，并提示“含退货 / 冲销”。

品牌下钻页面支持按渠道类型、渠道名称多选筛选；筛选条件统一作用于汇总卡、趋势、渠道维度和商品排行。渠道类型表现、线上平台表现、渠道名称表现和商品排行表均支持点击列头进行升降序排序；商品排行的原始排名列不参与排序。

品牌下钻经营看板按“经营指标、销售趋势与渠道贡献、线上/线下渠道效率、完整维度明细”组织，并支持近30天、本月、本年和自定义日期。看板不得展示接口没有提供的库存、同比或预警数据。

品牌下钻标题区右上角提供“月度趋势、渠道效率、数据明细”分页切换；顶部经营指标保持常驻。“月度趋势”使用接口日趋势数据按自然月汇总净销售额和订单数。

月度趋势右侧为“销售贡献度分析”，按是否配置线上平台划分线上和线下，分别对比净销售数量、净销售额的占比与实际值。净销售额为 0 的渠道不进入贡献度面板；若筛选结果没有非零净销售额，贡献度面板隐藏，趋势图占满整行。

品牌下钻的“线上平台表现”只统计已配置线上平台的渠道，不展示“未设置”。未配置线上平台的渠道属于线下渠道，其销售额仍按渠道类型进入“销售办公室”等渠道类型维度。品牌下钻路由在左侧导航中继承“品牌销售分析”的选中状态。

品牌销售分析及品牌下钻使用深玫红、浅玫红品牌色。品牌下钻顶部不展示件均价；线上 TOP 渠道以线上平台为主标题、具体渠道名称为浅色副标题，不展示订单数；线下核心渠道标题下不展示说明备注。

品牌销售分析及品牌下钻默认时间口径为“本年”，列表进入下钻时传递当前时间口径。月度销售额波动趋势的净销售额和订单数均使用点间直线，不使用平滑曲线。

明细行数和订单数不是同一个指标：

```text
明细行数：COUNT(*)
订单数：COUNT(DISTINCT 订单编号)
```

### 渠道分析

路由：

```text
/sales/channel-analysis
```

接口：

```text
GET /api/v1/sales/channel-analysis
```

数据来源：

```text
渠道清单：ods.渠道列表
销售事实：ods.销售单明细账
关联条件：销售单明细账.销售渠道 = 渠道列表.渠道名称
时间字段：销售单明细账.下单时间
```

页面规则：

- 渠道清单必须全量来自 `渠道列表`。
- 使用左连接保留当前周期无销售数据的渠道。
- 销售单存在但渠道列表无法匹配的渠道，单独显示在“未匹配销售渠道”。
- 渠道类型、线上平台、负责人、授权状态来自 `渠道列表`。
- 页面使用“月度渠道信息、渠道表现、数据明细”三个分页，默认打开“月度渠道信息”和“本年”时间范围。
- 月度渠道信息按自然月展示净销售额与订单数直线趋势，并对比线上、线下的销售数量和净销售额贡献。
- 渠道分析统一使用销售明细账，以保证“本年”可以覆盖完整的年度历史数据，而不是受订单汇总表当前数据起始日期限制。
- “线上平台表现”只统计已配置线上平台的渠道，不展示“未设置”；线上平台未设置的渠道归入线下，在渠道清单中显示为“线下”。

页面内容：

- 销售额、订单数、销售数量、有销售渠道数、未匹配渠道数
- 月度净销售额与订单数趋势、线上线下渠道贡献
- 线上 TOP 渠道、线下核心渠道
- 渠道类型表现
- 线上平台表现
- 渠道清单
- 未匹配销售渠道

曾确认的近30天渠道结果：

```text
渠道列表总数：86
有销售渠道：57
已授权渠道：32
未匹配销售渠道：4
```

## 已确认的 ODS 数据表

### ods.销售单查询

已确认字段：

```text
标记
订单编号
订单状态
结算状态
销售渠道
处理时间
付款时间
发货仓库
物流公司
物流单号
网店订单号
发货时间
订单类型
应收合计
货品数量
货品摘要
合并备注
下单时间
渠道分类
实付金额
市
updatetime
```

订单状态中包含“取消”的记录不进入销售分析的 `销售单查询` 汇总口径，例如：

```text
已取消
已取消-被合并
已取消-被拆分
```

主要用途：

- 销售概览
- 销售明细
- 销售分析一级页面顶部统一汇总
- 渠道排行
- 渠道分析销售事实

### ods.销售单明细账

当前字段：

```text
订单编号
网店订单号
销售渠道
渠道分类
货品编号
货品名称
货品条码
物流公司
发货仓库
品牌
规格
数量
单价
优惠(折扣)
金额
货品成本
分摊后单价
分摊金额
分摊后金额
费用分摊
毛利
毛利率
零售价
含税价
不含税价
承诺发货时间
下单时间
付款时间
市
updatetime
统计时间
```

主要用途：

- 商品销售排行
- 品牌销售分析
- 品牌渠道下钻

时间规则：

```text
页面筛选必须使用下单时间。
不要使用付款时间或统计时间作为这些页面的筛选字段。
```

当前检查到的数据覆盖范围：

```text
下单时间最小值：2026-01-01 00:00:00
下单时间最大值：2026-05-30 23:59:52
数据行数：1,561,752
```

因此当前明细账页面默认近30天为：

```text
2026-05-01 至 2026-05-30
```

在明细账加载更新数据之前，自定义查询 2026 年 6 月会返回空数据。

### ods.渠道列表

已确认字段：

```text
渠道编号
渠道名称
分类
渠道类型
助记码
公司
部门
联系人
联系电话
创建时间
提成公式
负责人
分组
线上平台
是否授权
默认仓库
结算账户
业务员
备注
公众号名称
公众号链接
货主
渠道ID
渠道类型编码
结算类型
线上平台编码
公司ID
授权Token
Token是否过期
省
市
区县
街道
办公地址
updatetime
```

主要用途：

- 渠道分析
- 品牌渠道下钻
- 渠道类型、线上平台筛选项
- 渠道负责人、授权状态等维度信息

已确认信息：

```text
数据行数：86
不同渠道名称：86
主要渠道类型：销售办公室、直营网店、分销办公室、直营门店
```

## 已知数据注意事项

1. `销售单查询` 和 `销售单明细账` 的数据粒度不同，订单数和销售额不一定天然一致。
2. 一级页面顶部汇总统一使用 `销售单查询`，商品、品牌维度明细使用 `销售单明细账`。
3. `销售单查询` 的销售分析口径需过滤订单状态包含“取消”的记录。
4. `分摊后金额`、`数量`可能为负数，表示退货或冲销，不应直接过滤。
5. 品牌下钻页面应显示“净销售额”，避免把负数误解为计算错误。
6. `COUNT(*)` 是明细行数，`COUNT(DISTINCT 订单编号)` 是订单数，两者不可混用。
7. 渠道关联统一使用渠道名称；无法匹配的销售渠道必须单独提示。
8. Windows 终端可能把 UTF-8 中文显示成乱码，但源文件和浏览器页面仍是正常中文。

## 当前销售分析菜单

```text
/sales/overview                 销售概览
/sales/detail                   销售明细
/sales/product-rank             商品销售排行
/sales/brand-analysis           品牌销售分析
/sales/brand-analysis/:brand    品牌渠道下钻
/sales/channel-analysis         渠道分析
```

## 库存分析数据源

### ods.分仓库查询

主要用途：

- 库存概览及仓库可用库存排行
- 库存周转
- 滞销商品
- 仓库名称筛选项

关键字段：

```text
仓库
货品编号
货品名称
品牌
货品分类
库存数量
可用库存
库存金额
近30天销量
近90天销量(库存公式)
updatetime
```

### ods.批次货品库存查询

主要用于批次库存、临期批次统计和 FEFO（先到期先出）分析。仓库筛选使用字段 `仓库`，FEFO 顺序使用字段 `到期日期`。

### 库存口径注意事项

1. `总库存查询.仓库` 当前只有“全部仓库”，不能作为真实仓库筛选来源。
2. 需要按仓库统计时，库存数量、可用库存和库存金额统一使用 `分仓库查询`。
3. 库存上下限来自 `总库存查询`，按 `货品编号` 与筛选后的分仓库存汇总关联。
4. 仓库可用库存排行按 `SUM(可用库存)` 降序。
5. 化妆品有效期通常按三年理解，但页面优先使用数据表原始 `到期日期`，不覆盖或重新推算。
6. FEFO 在同仓库、同货品内按 `到期日期` 升序；缺少到期日期的批次排在最后。
7. 库存分析默认筛选“上海仓库新”和“【商家仓】抖超上海仓”；页面中的货品条码筛选使用字段 `条码` 模糊查询，不等同于 `货品编号`。
8. 库存周转、滞销商品和批次效期页面可按 `货品编号` 下钻商品库存详情，详情继承当前仓库筛选，并展示分仓库存和批次 FEFO 顺序。
9. 库存筛选条件写入 URL；“恢复默认”回到两个默认仓库，“清空”表示不限制仓库。
10. 库存周转、滞销商品、批次效期和库存健康度使用数据库分页，接口参数为 `page`、`page_size`。
11. 库存健康度异常按优先级互斥归类：负库存、可用库存为零、不足14天、缺少条码、90天无销量、预计库存超过180天。
12. 所有库存分析页面统一提供“货品分类”多选筛选，默认选择“正装”和“小样”；清空分类表示不限制。分仓维度使用 `分仓库查询.货品分类`，总库存维度使用 `总库存查询.货品分类`，批次效期维度使用 `批次货品库存查询.货品分类`。
13. 库存周转、滞销商品、库存健康度的明细表，以及批次效期的 FEFO 和长期库存明细表，均需展示“货品分类”列。
14. 库存周转页使用“品牌周转、商品周转”页内分页，默认先进入品牌周转；品牌周转默认展示最近一个已完整结束的季度，例如 2026-07 默认 2026 Q2、2027-01 默认 2026 Q4。前端和后端接口必须使用同一动态规则，品牌默认不设筛选、展示全部品牌。
15. 品牌周转销售侧使用 `销售单明细账` 的净销售数量，库存侧使用 `分仓库查询` 的当前库存。当前没有历史库存快照，因此第一版口径为：`估算周转次数 = 季度净销售数量 / 当前库存数量`，`估算周转天数 = 季度天数 / 估算周转次数`。页面和接口必须标记 `basis=ending_stock_proxy`，不得表述为标准平均库存周转率。
16. 品牌周转继承库存默认仓库以及“正装、小样”货品分类筛选；仓库筛选在销售明细账中对应 `发货仓库`，在分仓库存中对应 `仓库`。
17. 品牌周转的页面视觉沿用品牌销售分析下钻：玫红主题、紧凑横向筛选、左侧标题标识、右侧“周转概览 / 品牌对比 / 数据明细”页内分页；品牌主题变量只在品牌周转视图生效，不影响商品周转。
18. 品牌周转横向条形图使用接口返回的 `chart_rows` 展示当前筛选下所有可计算品牌，不做 Top 截断；明细表继续独立分页。
19. 品牌周转提供按品牌汇总后的“当前库存门槛”筛选，选项为不限、至少 100、500、1,000、5,000 件，默认至少 100 件；接口参数为 `min_stock`，汇总指标、完整图表数据和分页明细必须使用同一过滤结果。
20. “品牌估算周转天数”图表暂不展示品牌为“未归类”的数据；汇总指标和数据明细仍保留该数据，避免改变整体计算口径。
21. 商品周转默认筛选“正装、小样”，并提供与品牌周转一致的当前库存门槛选项，默认至少 100 件；接口参数为 `min_stock`，按货品和仓库聚合后的库存数量执行过滤。
22. 品牌周转概览在用户筛选品牌后，以左中右三个横向条形图展示货品周转：左侧为“正装 + 小样”货品 Top 10，中间为“正装”货品 Top 10，右侧为“小样”货品 Top 10。每个面板标题区展示该分类全部货品的可用库存汇总和平均周转天数；平均周转按分类汇总口径 `季度天数 × 分类可用库存合计 / 分类净销售数量合计` 计算，不使用货品周转天数的算术平均。条形按具体货品可用库存降序排列，条形长度表达可用库存，但坐标轴、条形标签和悬浮提示不显示单品库存数量，只显示货品名称与估算周转天数。货品级估算公式为 `季度天数 / (季度净销售数量 / 可用库存)`；未筛选品牌时提示用户先选择品牌，品牌整体周转仍沿用当前库存口径。
23. 品牌周转筛选栏的品牌使用可搜索、可清空的下拉选择，不要求手工输入；品牌选项由 `/api/v1/inventory/warehouses` 响应中的 `brands` 提供，合并分仓库存与销售明细账的非空品牌并去重。选中品牌后，库存和销售查询必须按标准化品牌名称精确匹配，不能使用包含式模糊匹配，否则会把名称中包含该品牌的其他品牌一并汇总。
24. 品牌周转“数据明细”分页除品牌汇总表外，还需展示品牌内货品分类周转明细，并提供“正装 + 小样 / 正装 / 小样”三个维度切换。货品表展示货品名称、货品编号、货品分类、可用库存、季度净销售、估算周转天数和状态，支持列排序及独立分页。

## AI决策与模型配置

1. AI决策中心路由为 `/ai/decisions`，数据接口为 `/api/v1/ai/inventory-decisions`。
2. 核心销售、库存数字和异常分类由 SQL 与规则计算，模型只负责摘要、排序表达和行动建议，不允许模型自行编造指标。
3. 模型不可用时保留规则决策，页面显示“规则模式”。
4. 当前模型使用 OpenAI Compatible 协议，配置在系统设置的“模型设置”页面维护。
5. API Key 在后台管理库中加密保存；查询接口只返回掩码，不返回完整密钥。
6. AI库存决策缓存时间为5分钟，“重新生成决策”会绕过缓存并重新调用模型。
7. AI决策证据包含销售与库存两个维度：销售侧来自 `销售单查询`，按 `下单时间` 取近30天并过滤取消订单；库存侧来自 `分仓库查询` 和 `批次货品库存查询`。

## 企业知识与数据问答 MVP V1

### 项目定位

本版本由一名业务负责人和一名开发人员共同完成，目标是在现有 BI 中建设一个
可实际使用、可用于学习企业级 RAG 工程链路的最小版本。

MVP 不是通用聊天机器人，也不把订单、库存等实时事实数据向量化。系统统一处理：

1. 业务知识问题：指标定义、数据口径、页面说明和分析方法。
2. 经营指标问题：调用现有销售、库存和到货接口获得可信结果。
3. 探索式数据问题：复用现有 Text-to-SQL，通过只读 MySQL 查询回答。

回答中的业务结论必须能够追溯到知识来源、BI 指标或实际执行的 SQL。模型只负责
理解问题、选择工具和组织表达，不作为销售、库存、金额等事实数字的来源。

### V1 成功标准

V1 完成时必须满足：

1. 用户可以在一个新的“智能分析助手”页面连续提问并看到流式回答。
2. 系统能够正确处理知识、指标、Text-to-SQL 三类问题。
3. 知识回答展示文档标题、章节和版本；数据回答展示统计时间和数据更新时间。
4. Text-to-SQL 继续执行现有只读校验、表限制、超时和最大返回行数限制。
5. 会话、消息、运行状态和引用在服务重启后仍可查询。
6. 文档修改后可以重新入库，新版本验证成功后再替换旧版本。
7. 至少准备 50 个真实问题作为验收集，其中知识、指标、SQL 问题均有覆盖。
8. 权限过滤和 SQL 安全测试必须全部通过，不允许因 RAG 绕过现有数据权限。
9. 当前销售、库存、到货页面和接口不得因本功能发生口径或性能回退。

### V1 技术选型

```text
前端：Vue 3 + Element Plus
API：FastAPI
RAG 组件：LangChain
工作流：LangGraph
业务事实数据：现有 MySQL ODS/ADS，只读
AI 控制面数据：PostgreSQL
向量检索：Qdrant
缓存与任务队列：Redis
异步任务：Celery
流式返回：SSE
模型：现有 OpenAI Compatible 配置
部署：Docker Compose
```

存储边界：

```text
MySQL
  - ODS 业务事实
  - ADS 在线分析结果
  - 不迁移到 PostgreSQL

PostgreSQL
  - 知识库、文档和版本
  - 入库任务
  - 会话、消息、Run 和引用
  - 用户反馈和审计
  - LangGraph Checkpoint

Qdrant
  - 文档 Chunk
  - 向量和检索 Payload

Redis
  - Celery Broker
  - 短期缓存
  - 限流和分布式锁
```

首版 Chat Model、Embedding Model 和后续可选的 Reranker 使用外部模型接口，不在
当前生产服务器本地运行模型。

### 服务器资源约束

当前服务器约 15 GB 内存，已有 MySQL、DataEase、DolphinScheduler、APISIX 和 BI
容器。V1 只部署核心组件，不在同一服务器部署本地大模型、完整 Langfuse、
Prometheus、Grafana、MinIO 或 OCR 服务。

初始资源上限建议：

```text
PostgreSQL：512 MB 至 1 GB
Qdrant：1 GB 至 1.5 GB
Redis：256 MB 至 384 MB
Celery Worker：512 MB 至 768 MB，并发数为 1
RAG API 增量：不超过 512 MB
```

知识批量入库和离线评测安排在业务低峰期。部署前必须检查 CPU 核数、磁盘剩余空间、
Docker 数据目录和最近 7 天内存峰值。PostgreSQL、Redis 和 Qdrant 只允许通过
Docker 内部网络访问，不向公网暴露端口。

### V1 功能范围

#### 包含

1. Git 管理的 Markdown 和 YAML 知识。
2. 文档哈希去重、解析、切分、Embedding、索引和版本切换。
3. Qdrant Dense 向量检索和元数据过滤。
4. 基于用户角色和知识库的检索权限过滤。
5. LangGraph 问题路由和工具调用。
6. 知识检索、现有 BI 指标接口、Text-to-SQL 三类工具。
7. FastAPI SSE 流式输出。
8. 会话历史、引用来源、SQL 和表格结果展示。
9. PostgreSQL 中的运行记录、错误信息和用户反馈。
10. 基于 pytest 的固定问题回归测试。
11. 结构化日志，记录 Run、节点、检索数量、模型耗时、Token 和错误类型。

#### 不包含

1. PDF、DOCX、图片、扫描件和 OCR 入库。
2. 面向业务人员的完整知识库上传和审批后台。
3. 本地 LLM、Embedding 或 Reranker 模型。
4. Graph RAG、知识图谱和多 Agent 协作。
5. 自动执行补货、调价、删除或修改业务数据。
6. 自动采集互联网内容。
7. Kubernetes、微服务拆分和多节点 Qdrant。
8. 完整 Langfuse、Prometheus、Grafana 监控平台。
9. 将订单、库存明细等高频变化的业务事实写入向量数据库。

以上能力根据真实使用反馈进入 V1.1 或 V2，不得阻塞 V1 上线。

### 首版知识范围

项目新增以下目录：

```text
docs/knowledge/
  metrics/          指标定义
  tables/           表和字段语义
  pages/            BI 页面说明
  sql-examples/     已审核 SQL 示例
  playbooks/        经营分析流程
  faq/              标准问答
  evaluation/       黄金问题与期望结果
```

首版知识目标：

```text
核心指标定义：20 条
关键数据表说明：15 条
已验证 SQL 示例：20 条
BI 页面说明：15 条
经营分析手册：10 条
标准 FAQ：20 条
黄金验收问题：50 条
```

可以从现有 `AGENT.md`、`AGENTS.md`、`docs/`、Text-to-SQL 元数据、后端测试和
前端页面中生成知识草稿，但必须由业务负责人审核后才能进入 active 知识版本。
不得直接把整个源码目录切块写入 Qdrant。

#### 指标知识模板

```yaml
id: inventory_turnover_days
name: 品牌估算周转天数
definition: 使用季度净销售数量和当前可用库存估算库存可维持天数
formula: 季度天数 × 当前可用库存 / 季度净销售数量
data_source:
  - ads 库品牌周转数据
dimensions:
  - 品牌
  - 货品分类
  - 仓库
default_filters:
  - 货品分类为正装、小样
  - 当前库存不少于100件
caveats:
  - 当前没有历史平均库存，因此不是标准平均库存周转率
  - 周转天数越长表示周转越慢
owner: 业务负责人
status: active
updated_at: YYYY-MM-DD
```

#### 数据表知识模板

```yaml
id: ods_sales_order_detail
table: ods.销售单明细账
purpose: 商品、品牌和渠道维度的销售明细分析
date_field: 下单时间
business_keys:
  order: 订单编号
  sku: 货品编号
rules:
  - 负数量和负金额参与净额计算
  - 订单数使用订单编号去重
  - 页面日期筛选使用下单时间
forbidden_usage:
  - 不使用统计时间代替下单时间
owner: 业务负责人
status: active
updated_at: YYYY-MM-DD
```

### LangGraph V1 工作流

V1 使用单个状态图，不拆成多个 Agent：

```text
接收问题
  → 校验用户和权限
  → 规范化问题与会话上下文
  → 识别 knowledge / metric / sql / mixed
  → 按路由执行知识检索或业务工具
  → 检查证据和执行结果
  → 生成带引用的回答
  → 保存消息、Run、引用和 Trace
```

节点建议：

```text
authorize
normalize_question
route_intent
retrieve_knowledge
call_metric_tool
generate_sql
validate_sql
execute_sql
check_evidence
compose_answer
persist_result
```

`mixed` 问题允许同时执行知识检索和一个数据工具，但 V1 不允许模型无上限循环调用
工具。单次 Run 的工具调用次数、模型调用次数和总超时必须配置上限。

### 检索与引用约定

1. V1 先实现 Dense 检索，候选 Top K 和最终上下文数量必须可配置。
2. 检索前必须加入知识库、状态和角色过滤。
3. Chunk Payload 至少包含：

```text
knowledge_base_id
document_id
document_version
chunk_id
title
section_path
source_path
allowed_roles
content_hash
updated_at
```

4. 回答引用至少返回文档标题、章节、版本和源文件路径。
5. 检索不到足够证据时明确说明，不使用模型常识补写内部业务口径。
6. V1.1 再根据 50 个黄金问题的检索结果决定是否增加 Sparse 检索和 Reranker，
   不为技术栈展示而提前增加复杂度。

### API 范围

新增接口统一使用 `/api/v1/rag`：

```text
POST   /api/v1/rag/conversations
GET    /api/v1/rag/conversations
GET    /api/v1/rag/conversations/{conversation_id}

POST   /api/v1/rag/conversations/{conversation_id}/runs
GET    /api/v1/rag/runs/{run_id}
GET    /api/v1/rag/runs/{run_id}/events
POST   /api/v1/rag/runs/{run_id}/cancel

POST   /api/v1/rag/feedback

POST   /api/v1/rag/admin/reindex
GET    /api/v1/rag/admin/jobs
GET    /api/v1/rag/admin/jobs/{job_id}
```

SSE 事件范围：

```text
run.started
node.started
retrieval.completed
tool.started
tool.completed
answer.delta
citation
warning
run.completed
run.failed
```

前端当前通过本地 Token 认证，原生 `EventSource` 不方便设置 Authorization Header。
V1 使用 `fetch + ReadableStream` 消费 `text/event-stream`，不把长期访问令牌放入 URL。

### 前端范围

新增路由：

```text
/ai/assistant    智能分析助手
```

页面包含：

1. 会话列表。
2. 问题输入和流式答案。
3. 停止生成和重新提问。
4. 引用来源。
5. SQL 折叠面板。
6. 数据表格结果。
7. 统计时间和数据更新时间。
8. 点赞、点踩和文字反馈。

V1 不实现独立知识库管理页面。知识通过 Git 文件维护，由开发执行入库命令；
V1.1 再增加业务可使用的上传、预览、审核和发布页面。

### 版本实施计划

计划按 4 个阶段实施，每个阶段完成后必须保持系统可运行。

#### M1：基础设施与知识模板

目标：

1. Docker Compose 增加 PostgreSQL、Qdrant 和 Redis。
2. 后端增加连接配置、健康检查和 Alembic 迁移。
3. 建立 PostgreSQL 最小数据表。
4. 建立 `docs/knowledge/` 目录和知识模板。
5. 业务负责人完成首批 10 个指标和 5 张表的审核。

验收：

```text
现有 BI 正常运行
三个新增服务健康检查通过
服务重启后 PostgreSQL 和 Qdrant 数据仍存在
外部不能直接访问 5432、6333、6379
```

#### M2：知识入库与检索

目标：

1. 实现 Markdown/YAML 加载、规范化和结构切分。
2. 实现文件哈希、文档版本和幂等入库。
3. 使用 Celery Worker 执行 Embedding 和 Qdrant Upsert。
4. 实现角色过滤、检索调试接口和引用结构。
5. 完成首批 20 个指标、15 张表和 20 个 FAQ。

验收：

```text
重复入库同一文件不会产生重复 active Chunk
修改文件后生成新版本，旧版本在切换前仍可使用
不同角色无法检索未授权 Chunk
30 个知识测试问题中至少 27 个能在 Top 5 命中正确文档
```

#### M3：LangGraph 与数据工具

目标：

1. 建立 LangGraph 状态和路由。
2. 接入知识检索工具。
3. 将现有销售、库存接口封装为只读指标工具。
4. 将现有 Text-to-SQL 封装为受限工具。
5. 保存会话、Run、节点状态、引用和错误。
6. 实现 SSE 流式事件和任务取消。

验收：

```text
知识、指标、SQL 各 10 个问题能够走到正确路由
混合问题能够同时返回业务定义和实际数据
所有数据数字都来自指标工具或 SQL 结果
SQL 非只读、超时和超行数测试全部被拦截
后端重启后可以查询已完成会话和 Run
```

#### M4：前端、评测与上线

目标：

1. 完成 `/ai/assistant` 页面。
2. 展示流式回答、步骤、引用、SQL 和表格。
3. 完成 50 个黄金问题。
4. 增加结构化 Trace 和基础统计。
5. 在当前服务器完成受限资源部署、备份和回滚说明。

验收：

```text
50 个黄金问题整体路由准确率不低于 90%
知识问题 Hit@5 不低于 90%
引用展示准确率不低于 95%
权限泄漏和危险 SQL 用例为 0
普通知识问答首个流式事件 P95 不超过 3 秒
连续运行 24 小时无容器 OOM 和现有 BI 性能明显回退
```

### 两人协作分工

业务负责人负责：

1. 确认指标定义、默认筛选、数据限制和经营分析流程。
2. 审核模型生成的知识草稿，决定是否发布。
3. 提供真实问题和期望答案。
4. 验证回答是否符合业务语义，不只检查文字是否通顺。
5. 对错误回答标注为口径错误、数据错误、检索错误或表达问题。

开发人员负责：

1. 基础设施、数据模型、入库、检索和 LangGraph 工作流。
2. 指标工具与 Text-to-SQL 安全边界。
3. SSE、前端页面、任务状态、审计和部署。
4. 将业务验收问题固化为自动化测试。
5. 记录检索、模型、工具、SQL 和耗时，定位失败原因。

双方每个里程碑只进行一次集中验收。业务问题和口径修改统一记录到
`docs/knowledge/`，避免仅通过聊天或口头方式维护。

### 测试与评测

黄金问题文件至少包含：

```yaml
- id: eval-001
  question: 品牌周转天数为什么不是标准库存周转率？
  expected_route: knowledge
  expected_sources:
    - metrics/inventory_turnover_days.yaml
  required_points:
    - 当前使用期末库存代理
    - 当前没有历史平均库存
  forbidden_points:
    - 表述为标准平均库存周转率
  role: analyst
```

V1 自动化测试至少覆盖：

1. 文档哈希与版本切换。
2. Chunk 元数据和角色过滤。
3. 检索 Top 5 命中。
4. 问题路由。
5. 指标工具参数映射。
6. Text-to-SQL 只读校验。
7. Run 状态和错误恢复。
8. SSE 事件顺序和结束事件。
9. 引用与最终答案关联。
10. 模型不可用和数据库超时降级。

后端修改至少执行对应模块 `py_compile` 和单元测试；前端修改至少执行
`npm.cmd run build`。涉及交互时必须在浏览器中验证流式输出、停止任务、
引用抽屉、SQL 折叠和错误状态。

### 知识维护流程

V1 使用 Git 审核发布：

```text
业务提出新增或修改
  → 在 docs/knowledge 中创建草稿
  → 业务负责人审核口径
  → 提交 Git
  → 执行入库任务
  → 运行相关黄金问题
  → 验证通过
  → 切换 active 版本
```

知识状态：

```text
draft → indexing → validating → active → archived
```

实时业务事实继续通过 MySQL/ADS 查询。以下内容禁止作为普通知识 Chunk 入库：

1. 单笔订单和销售明细。
2. 每日完整库存快照。
3. 当前销售额、库存量、排名等动态结果。
4. API Key、访问令牌、客户联系电话和其他敏感信息。

### V1.1 候选范围

只有 V1 上线并收集真实反馈后才评估：

1. 知识库上传、预览、审核和发布后台。
2. PDF、DOCX 解析。
3. Sparse + Dense 混合检索。
4. Reranker。
5. Langfuse 或 OpenTelemetry 完整接入。
6. Redis Cache 与 Celery Broker 拆分。
7. 模型费用、Token、P95/P99 看板。
8. 从用户反馈一键生成 FAQ 或评测用例。

V1.1 的优先级由 V1 的错误分布决定：检索召回不足时增加混合检索，排序不准时
增加 Reranker，业务维护成本高时优先建设知识管理后台。

### 当前实施进度

截至 2026-07-30：

1. M1 代码已完成：RAG Compose、PostgreSQL 控制面、Alembic、Redis、Qdrant、
   健康检查和知识目录已经建立。
2. M2 工程链路已完成：Markdown/YAML 加载、LangChain 结构切分、内容哈希、
   幂等版本、Celery Worker、OpenAI-compatible Embedding 适配、Qdrant 入库、
   旧版本归档、角色过滤和检索调试接口已经建立。
3. 品牌估算周转天数和销售单明细账两条知识已由业务确认并标记为 `active`。
4. M2 本地自动化测试已覆盖加载、切分、批处理、版本切换、失败记录、删除归档和
   角色过滤。
5. M2 本地联调已完成：独立 Embedding 服务调用成功，PostgreSQL、Redis、Qdrant
   和 Celery Worker 已启动，首次迁移与入库成功；2 份 active 知识生成 2 个向量点，
   重复入库均判定为 unchanged。
6. 两道黄金问题 Top 5 均命中预期来源，且预期来源均位于第 1 名；后端 39 项自动化
   测试、Python 编译、Alembic 离线 SQL 和 Compose 配置校验均通过。
7. 当前尚未部署到远程生产服务器；下一阶段先在本地推进 M3 问题路由、LangGraph
   工作流、引用和运行记录，达到可演示状态后再安排服务器部署。
8. M3 第一切片已完成：LangGraph 单图实现问题规范化、knowledge / metric / sql /
   mixed 路由、知识检索、证据阈值、MiMo 回答和引用过滤；会话、Run、消息及引用已
   持久化到 PostgreSQL，并通过一次真实知识问答验收。
9. M3 第二切片已完成：销售概览和品牌周转已封装为 ODS 只读指标工具，支持日期、
   季度、品牌、正装/小样和最低库存参数；mixed 问题通过代码拆分口径与数据子问题，
   工具参数和精简结果保存到 Run Trace。默认销售条件、组合周转筛选及一次真实 mixed
   问答均已通过。受限 SQL、SSE、取消和逐节点耗时 Trace 留在后续 M3 切片。
