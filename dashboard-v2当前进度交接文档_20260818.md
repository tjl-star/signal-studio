# 视频运营数据看板交接文档

> 更新时间：2026-08-18
>
> 用途：在同一个项目中新开 Codex 对话后，先阅读本文档，再继续开发或核查。

## 一、项目位置

- 工作区：C:\Users\tjldq\Documents\数据看板搭建
- 当前看板：C:\Users\tjldq\Documents\数据看板搭建\dashboard-v2
- 页面入口：dashboard-v2\index.html
- 主要代码：dashboard-v2\app.js、dashboard-v2\style.css
- 数据目录：dashboard-v2\data
- 本地地址：http://localhost:8000/dashboard-v2/

启动方式：

```powershell
cd "C:\Users\tjldq\Documents\数据看板搭建"
python -m http.server 8000 --directory "C:\Users\tjldq\Documents\数据看板搭建"
```

如果 8000 端口已有服务，先确认服务实际指向的目录，避免打开旧版本页面。修改代码后使用缓存参数访问，例如：

```
http://localhost:8000/dashboard-v2/?v=20260818-handoff
```

## 二、最新且必须遵守的结论

### 1. 不做漏斗

上一次把多个 UV 字段强行串成漏斗，数据关系不成立，已经导致页面数据错误并被领导指出。

后续明确禁止：

- 不把不同字段强行连接成上下游漏斗。
- 不自行计算阶段转化率。
- 不根据数值大小猜测字段之间的链路关系。
- 不使用“看起来合理”的字段替代缺失字段。
- 不在页面上继续使用“频道页漏斗”“首页流量与转化漏斗”等容易造成误解的名称。

如果后台没有明确证明字段属于同一条用户链路，阶段转化率直接放弃。

### 2. Tab4 的产品形态

Tab4 最终应是：

> **频道运营分析看板**

它通过日期和频道筛选，展示后台直接提供的真实指标、前一日变化和趋势，不展示未经验证的漏斗。

页面名称建议使用“频道运营分析”，不要叫“频道页漏斗”。

### 3. 当前优先级

1. 数据字段和数据口径正确。
2. 页面明确展示真实字段来源。
3. 日期、频道筛选正确。
4. 绝对值和较前一日环比正确。
5. 最后才考虑图表样式和页面美化。

## 三、Tab4 最新需求

### 筛选

- 日期范围筛选，使用日历，不使用简单日期下拉框。
- 支持按频道筛选，例如电影、韩剧、泰剧、美剧、英剧、日剧、国产剧、精选等。
- 不需要新设备/老设备筛选。
- 不要擅自加入安卓、iOS、M站等客户端筛选，除非后续明确提出并确认数据口径。

### 页面展示

保留以下内容：

- 各核心指标 UV 绝对值。
- 各核心指标较前一日环比。
- 近期日趋势图。
- 必要时使用指标卡、折线图、柱状图等，不要把所有字段堆成大表格。

环比规则：

```
(当天值 - 前一天值) / 前一天值
```

环比展示只使用百分比：

- 增加：绿色向上箭头 + 百分比。
- 降低：红色向下箭头 + 百分比。
- 前一日没有数据、分母为 0 或口径无法比较：显示 --，不要猜算。

### 指标候选

这些字段可以作为候选，但必须逐个确认实际数据是否可靠，不是全部强行放入页面：

- 首页频道点击 UV
- 首页影视点击 UV
- 首页影视详情页尝试播放 UV
- 播放 5 分钟 UV
- 播放 10 分钟 UV，可选
- 有效播放 UV，仅在口径明确时使用
- 后台直接提供的整体转化率，可作为原始参考；不能改名为阶段转化率
- 人均播放时长，可作为观看深度指标；不属于漏斗阶段

由于当前数据已经发现层级数值可能反常，例如详情播放 UV 大于内容点击 UV，因此不应仅凭数值把它们串成漏斗。

## 四、已确认的数据问题

### 1. 频道数据不是严格嵌套关系

`dashboard-v2\data\home_funnel_requested_fields.json` 中存在类似情况：

- 电影频道某日：详情播放 UV 大于内容点击 UV。
- 国产剧频道某日：频道相关字段之间存在明显不符合漏斗直觉的数值关系。

这说明这些字段可能来自不同统计口径、不同事件集合或不同去重逻辑，不能直接用于阶段转化率。

### 2. Quick BI Excel 不能证明严格漏斗

文件：

```
C:\Users\tjldq\Downloads\首页各频道页看剧转化_首页上频道页的转化数据UV_20260818_1147.xlsx
```

该文件可用来核对字段名称和后台字段含义，但不是完整的按频道历史数据。文件中也出现有效播放 UV 与 5 分钟播放 UV 不完全符合嵌套关系的情况。因此不能直接据此构造频道漏斗。

### 3. 当前代码仍残留旧漏斗实现

`dashboard-v2\app.js` 中仍能看到旧的 `channel-funnel`、`traffic-chain`、`tab4RenderTrafficFinal` 等逻辑。这些代码代表历史尝试，不代表最新需求，也不能作为数据正确性的证明。

接手后重构 Tab4 时，应先清理或停用旧漏斗渲染，避免旧函数覆盖新函数或旧 DOM 与新 DOM 同时存在。

## 五、各 Tab 当前共识

### Tab1：大盘总览

- 展示全局用户规模、播放深度和客户端表现。
- 修改时不要恢复已经删除的重复模块。
- 客户端数据必须先按页面结束日期过滤，再取最新值。
- 安卓、iOS、M站的指标、标题和图表必须使用同一个筛选状态。

### Tab2：内容榜单

- 展示每日榜单快照。
- 主要分为总榜、新用户榜等业务榜单。
- 保留原始排名和原始字段，不合并同名内容，不重新计算排名。
- 昨日环比只保留数据和箭头：增加绿色，降低红色。
- 其他表头不要加上下排序标签；只有用户明确要求时再加。

主要来源：

```
dashboard-v2\data\站内播放排名Top30_20260706_20260804.json
```

### Tab3：搜索分析

- 热搜排行榜只需要总榜和新用户榜。
- 不需要拆分客户端或新老设备。
- 搜索词、搜索 UV、搜索 VV、昨日环比、内容类型、题材标签必须从真实数据读取。
- 如果原始搜索词为空，应检查原始 JSON、接口返回字段和字段映射，不能用其他字段补齐。
- 搜索整体转化数据缺失或口径不明时，显示暂无真实数据，不要自行制作搜索漏斗。

主要来源：

```
dashboard-v2\data\热搜Top30_20260706_20260804.json
dashboard-v2\data\热搜总榜_20260704_20260804.json
dashboard-v2\data\新用户热搜_词频_20260704_20260804.json
```

### Tab4：频道运营分析

- 彻底放弃旧漏斗形态。
- 先做真实 UV 指标、昨日环比和趋势。
- 频道筛选后，所有指标必须来自对应频道的数据。
- “全部频道”如果只是各频道 UV 相加，必须标明它不是跨频道去重用户数；无法确认时不要展示合计。
- 不做阶段转化率，除非后续取得后台明确的链路定义和同口径数据，并得到用户再次确认。

## 六、主要数据文件

Tab4 相关文件：

```
dashboard-v2\data\home_funnel_requested_fields.json
dashboard-v2\data\频道转化漏斗_20260804.json
dashboard-v2\data\home_channel_traffic_detail.json
dashboard-v2\data\home_sections_risk_detail.json
dashboard-v2\data\banner_click_detail.json
dashboard-v2\data\guess_you_like_home_data.json
dashboard-v2\data\guess_you_like_pv_home_data.json
```

唯一正式看板数据目录是：

```
C:\Users\tjldq\Documents\数据看板搭建\dashboard-v2\data
```

以后通过接口取得的数据，原则上整理到这个目录，并同步更新代码中的映射。不要再把同一份正式看板数据散落写入桌面文件夹；桌面文件可以作为临时下载或备份，但不是看板运行数据源。

## 七、数据更新原则

日维度更新不是只更新热播或热搜，而是整个看板都要支持日维度更新。

```
接口取得新日期数据
-> 按模块核对字段和口径
-> 追加到 dashboard-v2\data 对应 JSON
-> 检查是否重复日期
-> 检查日期范围和缺失字段
-> 页面按日期筛选读取
```

不要每天新建一套重复文件。文件名应以模块和覆盖日期命名；如果只是新增一天，可以追加到同一份结构稳定的历史数据文件，或者按既定模块建立清晰的日分片文件，但必须同步维护映射和说明。

重要原则：

- 不修改原始数值来“修正”异常。
- 不填 0 掩盖缺失。
- 不用 UV 替代 PV。
- 不用相近字段替代缺失字段。
- 发现异常时保留原始数据，并记录数据质量问题。

## 八、下一次 Codex 对话的工作顺序

### 第一步：确认代码实际生效逻辑

```powershell
cd "C:\Users\tjldq\Documents\数据看板搭建\dashboard-v2"
node --check app.js
```

检查 `app.js` 中最终生效的 `renderPage`、Tab4 构建函数和旧的 `tab4RenderTrafficFinal`。不要直接在文件末尾继续叠加覆盖逻辑。

### 第二步：核对 Tab4 数据

逐个确认：

1. 日期字段和可用日期范围。
2. 频道字段是否真的随频道筛选变化。
3. 每个 UV 字段的后台定义。
4. 当前日和前一日是否都存在。
5. 是否存在重复记录、空字段和异常值。
6. 是否真的支持按频道导出，而不是把全站数据复制到每个频道。

### 第三步：先做无漏斗版本

建议先做最小可用版：

- 日期范围日历。
- 频道选择。
- 4 个左右核心 UV 指标卡。
- 每个指标的较前一日环比。
- 1 个 UV 趋势图。
- 数据为空时明确显示暂无真实数据。

暂时不要做：

- 漏斗图。
- 阶段转化率。
- 自动推算的转化路径。
- 未经确认的全频道排名。

### 第四步：验证

用至少两个频道、连续两天数据验证：

- 选择频道后数值是否切换。
- 环比是否按同一频道、前一天计算。
- 日期范围是否只影响趋势和当前指标。
- 空数据、分母为 0 时是否显示 --。
- 页面没有出现“漏斗”或“阶段转化率”字样。

## 九、给新 Codex 的明确指令

开始工作前请先阅读本文件，并遵守：

> 当前目标不是把所有后台字段搬到页面，也不是重新制作漏斗。
>
> 当前目标是先做一个数据口径可信的“频道运营分析”页面。
>
> 如果数据链路无法证明，就只展示原始 UV、原始后台指标、较前一日环比和趋势；不要自行推导阶段转化率。

