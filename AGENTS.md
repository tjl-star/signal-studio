# Signal Studio 项目级 Agent 规则

## 1. 当前目标

本项目已经完成旧静态看板到可维护本地单机运营数据产品的首轮重构。`dashboard-app/` 是唯一正式应用，历史页面代码和基线不再保留。

目标架构固定为：

```text
data_provider / Quick BI MCP
        -> Python 增量同步与校验
        -> SQLite
        -> FastAPI
        -> Vue 3 + TypeScript + Vite + ECharts
```

当前阶段保持业务指标和数据口径稳定，优先完善真实数据同步、质量校验和本地运维。项目不建设 ADS、数仓版本快照、微服务、Redis、Celery、消息队列或分布式调度体系；只有经过真实容量或并发证据证明 SQLite 不满足要求后，才评估更重的数据基础设施。

## 2. 本地交付边界

项目当前没有服务器部署目标，默认交付为 Windows 本地一体化应用：

1. 开发环境允许 Vite 与 FastAPI 分别运行。
2. 发布环境先构建 Vue；由 FastAPI 同源托管前端 `dist`、`/api` 和导出文件。
3. SQLite 数据库、日志和人工映射文件存放在代码目录之外的持久化运行目录；升级程序不得覆盖用户数据。
4. 提供一个启动脚本完成环境检查、数据库迁移、服务启动、健康检查和浏览器打开。用户只访问 `127.0.0.1`，不要求 Nginx、Docker、域名、HTTPS 或云数据库。
5. 服务默认只绑定 `127.0.0.1`。局域网共享、开机启动、安装包或服务器部署均属于后续独立需求，不得在普通重构任务中默认引入。
6. 本地发布必须可备份和恢复 SQLite 文件、人工映射与配置；不得把凭证写入前端、数据库、仓库或日志。

## 3. 架构约束

### 后端

- 使用 FastAPI、SQLAlchemy 2、Alembic 和 Pydantic。
- 路由只处理协议与参数；业务计算进入 service；SQL 与持久化进入 repository。
- SQLite 启用 WAL、外键、合理的 `busy_timeout` 和事务写入。
- 同步任务采用“抓取 -> 临时写入 -> 数据质量校验 -> 事务发布 -> 记录批次”的流程；失败时保留上一份可用数据。
- 明细事实只保存一份；周、月、榜单等派生结果通过查询或轻量汇总表生成，不复制完整历史版本。
- 所有列表接口使用服务端分页或有明确的行数上限，前端不得下载全量明细后再分页。

### 前端

- 使用 Vue 3、TypeScript、Vite、Vue Router、Pinia 和 ECharts；统一界面模块可使用 Element Plus，但必须通过项目设计 Token 收敛视觉表现。
- 页面通过统一的数据 repository/interface 调用 FastAPI，不读取数据库，不知道 MCP 参数，也不直接引用快照文件路径。
- 业务计算使用纯函数或领域 module；页面 module 负责组合，不在渲染函数中混合取数、映射、计算、DOM 拼接和导出。
- 筛选条件需要贯穿指标、图表、表格、导出、缓存键和 URL 状态。
- 每个页面必须覆盖加载、空数据、字段缺失、缓存数据、请求失败和重试状态。
- 不重新引入已删除的旧静态页面、演示版本或函数覆盖链；新增功能只进入当前分层架构。

## 4. UI/UX目标

产品定位为面向影视运营与数据人员的桌面端内部分析工具，强调可信、快速扫描和问题定位。

- 使用克制的企业数据产品风格：中高信息密度、单一主色、清晰层级、轻量动效。
- 建立统一的导航、筛选栏、指标摘要、图表、数据表格、数据状态和来源说明。
- 图表颜色、精度、Tooltip、图例、坐标轴、空值和导出规则必须统一。
- 大表使用服务端分页或虚拟滚动；不要一次渲染数千行。
- 页面显著位置展示数据截至日期、同步时间和数据状态；详细的数据源与口径进入统一说明抽屉。
- 重构期间保持现有主要导航名称和业务口径；信息架构调整必须单独验收，不能与数据迁移静默混合。

## 5. 强制数据协议

凡涉及数据查询、导出、核对、分析、接口接入、字段映射或数据库建模，必须先完整读取根目录的 [`DATA_SOURCE_INDEX.md`](DATA_SOURCE_INDEX.md)，再选择数据源。

完整字段映射、MCP 接入边界、路由与口径规则见 [`agent.md`](agent.md) 第 4、5 节。必须遵守：

1. 使用索引登记且当次实际可调用的数据源；未接入字段标记为“待接入”。
2. 不使用旧值、mock 值或相近字段替代目标指标，不跨接口拼接成不存在的页面口径。
3. 核对日期、客户端、平台、新老设备、页面、频道、UV/PV和去重粒度。
4. API结果附带 `source`、接口/组件、原始字段、查询日期、筛选条件、同步时间和数据状态元信息。
5. 人工映射独立、可审计、可版本化；保留原始值、展示值、修改原因和更新时间。

## 6. 实施顺序

默认采用渐进迁移：

1. 固化当前页面截图、关键指标与浏览器冒烟测试。
2. 建立 SQLite 模型、迁移和现有 JSON 一次性导入工具。
3. 建立 FastAPI 分层、健康检查和统一响应元信息。
4. 建立 Vue 应用壳、设计 Token 和共享界面模块。
5. 当前已迁移页面以自动化测试和真实数据抽查作为回归基线。
6. 新增数据接入先完成字段、日期、粒度和来源核验，再进入 SQLite 与页面。
7. 结构重构、业务口径变更和数据源切换分别提交、分别验收；不保留无引用旧实现。

## 7. 完成标准

一次重构任务只有在以下条件均满足时才算完成：

- 修改范围内的新旧指标已按同一日期和筛选条件核对。
- 数据库迁移、后端测试、前端测试和生产构建通过。
- 本地一体化启动后，健康检查和目标页面可访问。
- 加载、空数据、错误和字段缺失状态经过验证。
- SQLite、映射和配置升级后仍可恢复，且没有凭证进入仓库或前端产物。
- 未迁移页面保持可用；相关旧代码在迁移验收后被删除，而不是继续叠加兼容层。
