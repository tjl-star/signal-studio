# 项目级 Agent 规则

本项目所有涉及数据查询、导出、核对或分析的任务，必须先读取根目录的 `DATA_SOURCE_INDEX.md`，再选择并调用数据源。

完整规则、字段映射、数据源路由和口径校验要求见 [`agent.md`](agent.md) 第 3.5 节。不得使用未核验的接口、旧值、mock 值或相近字段替代目标数据；最终结果必须注明数据源、接口/组件、原始字段、日期和筛选口径。
