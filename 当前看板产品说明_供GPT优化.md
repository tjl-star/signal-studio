# 视频运营数据平台｜当前看板产品说明

> 文档用途：供后续 AI、产品、运营和开发人员理解当前最终生效的 `dashboard-v2` 页面。
>
> 口径：本文只描述当前 `dashboard-v2/index.html` 与最终生效的 `dashboard-v2/app.js` 所渲染的页面，不把旧版页面、历史设计稿、未接入接口或废弃 JSON 视为当前功能。
>
> 当前数据快照：大部分模块已更新至 2026-08-23；不同接口的数据可用日期可能不同，页面应以各模块实际数据日期为准。

## 一、看板整体介绍

### 看板名称

视频运营数据平台。

### 产品目标

通过统一看板查看视频业务的用户规模、内容热度、搜索行为、首页流量、频道转化、板块效果和 Banner 点击表现，帮助运营人员完成：

- 判断整体用户规模和播放健康度；
- 发现热播内容、热搜内容及新入榜内容；
- 分析搜索入口到内容播放的转化漏斗；
- 分析首页频道、板块、资源位的曝光与点击效果；
- 对比环比变化，定位增长或下降异常；
- 下钻到日期、客户端、频道、板块或资源位明细。

### 服务对象

- 内容运营：关注热播榜、热搜榜、剧种和题材表现；
- 产品运营：关注 DAU、搜索、首页入口和转化链路；
- 频道运营：关注频道 UV、详情播放、5 分钟播放和有效播放；
- 资源位运营：关注板块、Banner 的曝光、点击和点击率；
- 数据分析人员：核对接口字段、日期、客户端和环比口径。

### 当前版本范围

当前页面为本地 `dashboard-v2` 版本，入口为：

`http://127.0.0.1:8000/dashboard-v2/`

当前主导航包含 6 个 Tab：

1. 大盘总览
2. 内容榜单
3. 搜索分析
4. 首页频道运营分析
5. 首页板块运营分析
6. Banner 点击分析

## 二、当前 Tab 总览

| Tab 名称 | 页面入口 | 主要目标 |
|---|---|---|
| 大盘总览 | 左侧导航“⦿ 大盘总览” | 查看核心用户规模、播放率和消费深度 |
| 内容榜单 | 左侧导航“⊡ 内容榜单” | 查看总榜、新用户榜、播放 VV、排名和新入榜状态 |
| 搜索分析 | 左侧导航“⌕ 搜索分析” | 查看搜索使用、搜索转化和热搜榜表现 |
| 首页频道运营分析 | 左侧导航“◌ 首页频道运营分析” | 查看固定客户端 `android_rrsp_xb` 下各频道的播放转化表现 |
| 首页板块运营分析 | 左侧导航“▤ 首页板块运营分析” | 查看首页板块曝光、点击和环比变化 |
| Banner 点击分析 | 左侧导航“▧ Banner 点击分析” | 查看 Banner 位置、标题、客户端和点击率表现 |

## 三、每个 Tab 详细说明

## 1. 大盘总览

### 1.1 页面定位

用于快速判断整体业务健康度，先看核心用户规模，再看播放率、消费深度和客户端差异。

### 1.2 页面结构

从上到下包括：

1. 核心指标区域；
2. 用户规模趋势区域；
3. 播放率趋势区域；
4. 客户端拆分表；
5. 用户消费深度趋势区域。

### 1.3 筛选条件

| 筛选项 | 类型 | 影响范围 |
|---|---|---|
| 客户端 | 下拉框：安卓、iOS、M站 | 影响核心指标、趋势图、客户端对应数据 |
| 核心指标日期 | 日期选择器 | 影响核心指标卡片的当前日期 |
| 用户规模趋势日期 | 起止日期 | 影响 DAU、新增设备趋势图 |
| 用户消费深度日期 | 起止日期 | 影响播放率、人均播放时长、人均播放次数趋势图 |

页面默认使用已加载数据中的最新日期；各模块若数据范围不同，不应将缺失日期当作 0。

### 1.4 KPI 指标

| 指标名称 | 含义 | 数据来源 |
|---|---|---|
| 设备 DAU | 当日活跃设备数 | `coreData.device_dau` |
| 新增设备 | 当日新增设备数 | `coreData.new_device` |
| 播放率 | 后台直接返回的播放率 | `coreData.play_rate` |
| 人均播放时长 | 客户端总人均观看时长，单位分钟 | `perCapitaWatchDuration.total_avg_watch_duration` |
| 人均播放次数 | 客户端总人均播放次数 | `perCapitaPlayCount.total_avg_play_count` |

核心 DAU、新增设备和播放率的客户端口径为安卓、iOS、M 站；核心接口密码只通过临时环境变量传入，不写入代码或数据文件。

### 1.5 图表说明

#### 设备 DAU 趋势

- 类型：折线图；
- X 轴：日期；
- Y 轴：设备 DAU；
- 展示指标：`device_dau`；
- 目的：观察活跃设备规模走势和突变。

#### 新增设备趋势

- 类型：折线图；
- X 轴：日期；
- Y 轴：新增设备数；
- 展示指标：`new_device`；
- 目的：观察拉新规模变化。

#### 播放率趋势

- 类型：折线图；
- X 轴：日期；
- Y 轴：百分比；
- 展示指标：`play_rate`；
- 目的：观察活跃设备中的播放行为变化。

#### 用户规模趋势

- 类型：柱线组合图；
- X 轴：日期；
- 柱：设备 DAU；
- 线：新增设备；
- 目的：同时观察存量活跃和新增变化。

#### 用户消费深度趋势

- 类型：多轴组合图；
- X 轴：日期；
- 指标：播放率、人均播放时长、人均播放次数；
- 目的：观察用户从“活跃”到“播放”和“消费深度”的变化。

### 1.6 明细表说明

#### 客户端拆分

字段：客户端、设备 DAU、播放率、人均播放时长、人均播放次数。

该表按安卓、iOS、M 站展示最新可用日期的客户端数据，不提供独立排序或行筛选。

### 1.7 数据来源

| 页面字段 | 当前读取文件 | 接口/字段 |
|---|---|---|
| DAU、新增设备、播放率 | `dashboard-v2/data/new_people_video_daily_active.json` | `coreData` |
| 人均播放时长 | `dashboard-v2/data/人均播放时长_近30天_按客户端.json` | `perCapitaWatchDuration` |
| 人均播放次数 | `dashboard-v2/data/avg_play_count_by_client.json` | `perCapitaPlayCount` |

### 1.8 当前限制

- 核心指标必须有 `coreData` 访问密码；接口无数据时不能用其他指标推算；
- 人均播放时长和次数的日期范围取决于对应接口实际返回；
- 页面不会把缺失日期填充为 0；
- 客户端下拉框只支持安卓、iOS、M 站，不支持任意自定义客户端。

## 2. 内容榜单

### 2.1 页面定位

用于判断哪些内容正在获得播放贡献，哪些内容进入榜单，以及总榜和新用户榜的差异。

### 2.2 页面结构

从上到下包括：

1. 榜单摘要卡片；
2. 播放 VV Top10 横向排行图；
3. 榜单数据说明；
4. 站内播放排名明细表。

### 2.3 筛选条件

| 筛选项 | 类型 | 影响范围 |
|---|---|---|
| 榜单类型 | 切换按钮：总榜、新用户榜 | 影响榜单明细和排行图 |
| 日期 | 日期选择器 | 影响当前榜单日期 |
| 榜单状态 | 下拉框：全部、新入榜 | 只保留全部内容或动态识别的新入榜内容 |
| 排序 | 表头排序 | 支持排名、播放 VV、昨日环比等字段排序 |
| 环比方向 | 表头上/下箭头 | 只显示上升或下降记录，并按变化幅度降序 |

筛选状态互斥：切换榜单类型、榜单状态或环比方向时，会清除另外两类筛选，避免多个筛选条件叠加后出现空表。

“新入榜”按当前日期与前一日的内容名称比较动态判断；不能只依赖原始文件中的 `榜单状态` 字段。

### 2.4 KPI/摘要指标

| 指标 | 含义 |
|---|---|
| 榜单记录 | 当前榜单和日期的记录数 |
| 最高播放 VV | 当前榜单最高播放 VV |
| 播放 UV 状态 | 统计播放 UV 为 `--` 的记录数 |
| 数据日期 | 当前正在展示的榜单日期 |

### 2.5 图表说明

#### 播放 VV Top10

- 类型：横向柱状图；
- X 轴：播放 VV；
- Y 轴：内容名称；
- 展示：当前榜单前 10 条内容；
- 目的：快速识别播放量最高的内容。

### 2.6 明细表说明

表格名称：站内播放排名 Top30 明细。

字段：

| 字段 | 说明 |
|---|---|
| 日期 | 榜单日期 |
| 排名 | 原始榜单排名，不重新计算 |
| 内容名称 | 剧名/内容名称 |
| 播放 UV | 播放用户数；接口未返回时显示 `--` |
| 播放 VV | 播放次数 |
| 昨日环比 | 当前内容相对前一日的播放变化 |
| 聚集类型 | 原始剧种编码，例如 TH、KR、CHN |
| 内容分类 | 电视剧、电影等内容类型 |
| 题材标签 | 剧情、爱情、同性等标签 |
| 榜单状态 | 全部或动态识别的新入榜 |
| 数据状态 | 真实 Top30、字段缺失说明等 |

排序规则：默认按原始排名；点击表头可切换升降序；点击环比上箭头只显示上升，点击下箭头只显示下降，并按环比绝对值降序。

### 2.7 数据来源

当前页面读取：

`dashboard-v2/data/站内播放排名Top30_20260706_20260804.json`

对应接口：

- 总榜：`seasonPlayVV`，按 `play_count` 排序取 Top30；
- 新用户榜：当前文件中的新用户榜记录受接口返回范围限制；
- 内容元数据：`season_type`、`season_classify`、`plot_type` 等字段映射到页面列。

注意：文件名中的日期是历史命名，不代表当前数据只到 2026-08-04；文件会由增量脚本原地追加更新。

### 2.8 当前限制

- 当前播放榜接口没有播放 UV 字段，页面保留 `--`；
- 新用户榜接口的返回条数可能少于 Top30；
- 榜单状态需要基于前一日内容集合动态计算；
- 同名内容按原始内容记录保留，不自动合并。

## 3. 搜索分析

### 3.1 页面定位

用于分析用户从搜索入口、搜索点击、首帧播放到 5 分钟播放的转化过程，并查看热搜内容变化。

### 3.2 页面结构

从上到下包括：

1. 搜索 KPI 卡片；
2. 搜索转化链路；
3. 转化率趋势图；
4. 新老用户搜索行为趋势图；
5. 热搜榜明细；
6. 搜索榜单的总榜/新用户榜、日期、榜单状态和环比筛选。

### 3.3 筛选条件

| 筛选项 | 类型 | 影响范围 |
|---|---|---|
| 搜索榜单类型 | 总榜、新用户榜 | 影响热搜明细 |
| 日期 | 日期选择器 | 影响当前热搜日期 |
| 榜单状态 | 全部、新入榜 | 影响热搜明细 |
| 环比方向 | 上升/下降箭头 | 只显示对应环比方向 |
| 表头排序 | 点击表头 | 影响榜单排序，不改变原始数据 |

榜单筛选状态互斥，避免状态筛选和环比筛选叠加后出现空结果。

### 3.4 KPI 指标

| 指标名称 | 含义 | 数据来源 |
|---|---|---|
| 搜索点击转化率 | 搜索点击相关转化指标 | 搜索使用/搜索点击转化接口 |
| 首帧播放 UV 整体转化率 | 搜索用户到首帧播放 UV 的转化率 | 当前搜索转化快照字段 |
| 播放 5 分钟 UV 整体转化率 | 搜索用户到 5 分钟播放 UV 的转化率 | 当前搜索转化快照字段 |
| 新用户搜索使用率 | 新用户搜索点击 UV / DAU 口径 | 新用户搜索使用率快照 |
| 老用户搜索使用率 | 老用户搜索点击 UV / DAU 口径 | 老用户搜索使用率快照 |

### 3.5 图表说明

#### 搜索转化链路

- 类型：漏斗/链路卡片；
- 展示：搜索点击、首帧播放、5 分钟播放等阶段；
- 目的：观察搜索用户在各播放阶段的转化损耗。

#### 转化率趋势

- 类型：折线图；
- X 轴：日期；
- Y 轴：转化率；
- 展示指标：搜索使用或搜索点击相关转化率；
- 目的：观察搜索行为长期变化。

#### 新老用户搜索行为

- 类型：折线图；
- X 轴：日期；
- Y 轴：搜索使用率；
- 展示：新用户、老用户搜索行为趋势；
- 目的：识别新老用户搜索意愿差异。

### 3.6 明细表说明

#### 热搜明细

字段：排名、搜索词/剧名、搜索次数或搜索 VV、搜索 UV、昨日环比、内容类型、地区、剧种、题材标签、榜单状态。

排序支持：排名、搜索 VV、搜索 UV、昨日环比等。

### 3.7 数据来源

当前页面读取的主要文件：

- `dashboard-v2/data/热搜总榜_20260704_20260804.json`
- `dashboard-v2/data/新用户热搜_词频_20260704_20260804.json`
- 搜索使用率、首帧播放、5 分钟播放等搜索转化快照文件。

接口映射：

- 总榜剧集热搜：`seasonSearchAuthInfo`；
- 新用户热搜词：`newDeviceHotWords`；
- 搜索转化：`searchClickConversion` 及搜索转化快照；
- 剧集元数据：`seasonPlayVV` 或热搜关联元数据字段。

### 3.8 当前限制

- 热搜词榜不等同于剧集热搜榜；热搜词可能是“爱情”“我们”等非剧名词条；
- `newDeviceHotWords` 返回搜索词和搜索次数，不一定返回剧集 UV、内容类型、题材标签和剧种；
- 某些日期的 `seasonSearchAuthInfo` 可能为空，因此不能强行拼成完整剧集榜；
- Quick BI“搜索整体数据”中的部分字段不一定与 `dramaConversion` 字段同口径，禁止直接替代。

## 4. 首页频道运营分析

### 4.1 页面定位

用于分析首页频道入口进入后，各频道的点击、播放和有效播放转化，定位频道运营表现差异。

### 4.2 页面结构

当前页面主要包括：

1. 固定客户端和日期筛选；
2. 核心指标卡片；
3. 播放转化表现横向比较；
4. UV 趋势；
5. 转化率趋势；
6. 频道指标明细；
7. 频道效果对比。

### 4.3 筛选条件

| 筛选项 | 类型 | 影响范围 |
|---|---|---|
| 频道 | 下拉框 | 影响核心指标和单频道趋势 |
| 指标日期 | 日期选择器 | 影响当前核心指标、对比图和明细 |
| UV 指标 | 下拉框/标签 | 切换频道点击 UV、内容点击 UV、详情播放 UV、有效播放 UV 等 |
| 转化率指标 | 下拉框/标签 | 切换首帧、5 分钟、10 分钟、有效播放等转化指标 |
| 明细日期 | 日期选择器 | 影响频道指标明细 |
| 环比箭头 | 表头上/下箭头 | 只显示上升或下降记录，并按幅度排序 |

客户端固定为 `android_rrsp_xb`，当前页面不提供客户端切换。

### 4.4 KPI 指标

| 指标名称 | 含义 | 数据来源 |
|---|---|---|
| 首页频道点击 UV | 首页进入频道后的点击用户数 | `dramaConversion.tab_click_uv` |
| 首页影视点击 UV | 首页内容点击用户数 | `dramaConversion.content_click_uv` |
| 详情页尝试播放 UV | 进入详情页并尝试播放的用户数 | `dramaConversion.detail_play_uv` |
| 播放超过 5 分钟 UV | 播放超过 5 分钟用户数 | `dramaConversion.detail_play_5_mins_uv` |
| 有效看剧 UV | 有效播放用户数 | `dramaConversion.video_uv` |
| 人均播放时长 | 平均视频播放时长 | `dramaConversion.avg_video_time` |

### 4.5 图表说明

#### 播放转化表现横向比较

- 类型：横向柱状图；
- X 轴：转化率；
- Y 轴：频道；
- 展示：选定日期、选定转化率指标下的 8 个频道；
- 目的：比较频道之间的播放转化能力。

#### UV 趋势

- 类型：折线图或趋势图；
- X 轴：日期；
- Y 轴：UV 数值；
- 展示：选定 UV 指标和频道的历史变化；
- 目的：观察频道流量规模趋势。

#### 转化率趋势

- 类型：折线图；
- X 轴：日期；
- Y 轴：百分比；
- 展示：选定转化率指标和频道的历史变化；
- 目的：观察频道播放效率趋势。

#### 频道效果对比

- 类型：横向柱状图；
- 展示：选定日期下各频道的同一指标；
- 排序：按指标值从高到低；
- 目的：进行频道横向比较。

### 4.6 明细表说明

表格名称：频道指标明细。

字段包括：频道、首页频道点击 UV、首页影视点击 UV、详情页尝试播放 UV、有效看剧 UV、5 分钟 UV 转化率，以及各指标的较前一日变化。

交互规则：

- 点击上箭头：只显示绿色上升数据，并按涨幅降序；
- 点击下箭头：只显示红色下降数据，并按跌幅降序；
- 各环比列独立排序；
- 点击另一列时重新从完整数据集筛选，不在已筛选结果上二次破坏性过滤。

### 4.7 数据来源

当前读取：

`dashboard-v2/data/频道运营分析_dramaConversion_android_rrsp_xb_20260705_20260804.json`

接口：`dramaConversion`。

主要字段映射：`tab_click_uv`、`content_click_uv`、`detail_play_uv`、`detail_play_5_mins_uv`、`detail_play_10_mins_uv`、`video_uv`、`avg_video_time` 及对应转化率字段。

### 4.8 当前限制

- 客户端固定为 `android_rrsp_xb`；
- 页面展示的是接口原始转化字段，不在前端重新计算阶段转化率；
- 某些日期或频道可能没有接口返回，页面应显示空状态；
- `--`、无对比和零值需要区分，不能简单当成下降。

## 5. 首页板块运营分析

### 5.1 页面定位

用于定位首页各频道、板块和子板块的曝光与点击表现，并通过环比找出增长或下降明显的板块。

### 5.2 页面结构

从上到下包括：

1. 日期、频道和客户端口径说明；
2. 频道板块效果排行；
3. 板块明细表；
4. 分页和环比筛选。

### 5.3 筛选条件

| 筛选项 | 类型 | 影响范围 |
|---|---|---|
| 日期 | 日期选择器 | 影响排行和明细当前日期 |
| 频道 | 下拉框 | 过滤板块所属频道 |
| 指标 | 下拉框 | 切换排行指标，如曝光 UV、点击 UV、点击率 UV |
| 明细排序 | 表头按钮 | 对日期、频道、板块、曝光 UV、点击 UV、点击率等排序 |
| 环比方向 | 表头上/下箭头 | 只显示上升或下降的环比记录 |

固定客户端：`android_rrsp_xb`。

### 5.4 KPI/排行指标

| 指标 | 含义 |
|---|---|
| 曝光 UV | 看到板块的独立用户数 |
| 点击 UV | 点击板块的独立用户数 |
| 点击率 UV | 点击 UV / 曝光 UV 的 UV 点击率 |
| 较前一日变化 | 当前日期与前一可用日期的变化百分比或百分点 |

### 5.5 图表说明

#### 频道板块效果排行

- 类型：横向柱状图；
- X 轴：当前选定指标；
- Y 轴：频道/板块；
- 展示：当前日期下排名靠前的板块；
- 目的：快速发现高曝光、高点击或高点击率板块。

### 5.6 明细表说明

字段：日期、频道、板块/`group_name`、曝光 UV、较前一日变化、点击 UV、较前一日变化、点击率 UV、较前一日变化。

排序规则：

- 默认按点击 UV 或当前指标排序；
- 支持按日期、频道、板块、曝光 UV、点击 UV、点击率 UV排序；
- 环比上/下箭头按颜色和方向过滤；
- 明细支持分页；
- 异常数据保留在明细中，但不参与排行。

异常规则包括：点击 UV 大于曝光 UV、点击率超过 100%、曝光 UV 为 0 但仍有点击等。

### 5.7 数据来源

当前读取：

`dashboard-v2/data/home_section_ops_20260705_20260804.json`

接口：`sectionData`。

字段映射：`date`、`channel`、`group_name`、`exposure_user`、`click_user`、`ctr_uv`。

### 5.8 当前限制

- 客户端固定为 `android_rrsp_xb`；
- 异常数据不会被删除，只是不参与排行；
- 环比依赖当前日期和前一可用日期都有同口径记录；
- 数据缺失时不能用相邻日期或其他客户端代填。

## 6. Banner 点击分析

### 6.1 页面定位

用于分析 Banner 资源位的曝光、点击和点击率，帮助运营识别高效 Banner、位置和客户端。

### 6.2 页面结构

从上到下包括：

1. 日期与客户端筛选；
2. Banner 效果排行；
3. Banner 明细表；
4. 分页。

### 6.3 筛选条件

| 筛选项 | 类型 | 影响范围 |
|---|---|---|
| 日期 | 日期选择器 | 影响排行和明细 |
| 客户端 | 分段按钮：安卓、M站 | 影响排行和明细 |
| Banner 位置 | 下拉框 | 过滤排行或明细的位置 |
| 排行类型 | 标签：点击 UV TOP10、点击率 TOP10 | 切换排行指标 |

### 6.4 KPI/排行指标

| 指标 | 含义 |
|---|---|
| 曝光 UV | Banner 被看到的独立用户数 |
| 点击 UV | 点击 Banner 的独立用户数 |
| 点击率 | 点击 UV / 曝光 UV |
| 点击 UV TOP10 | 按点击 UV 排序的前 10 条 |
| 点击率 TOP10 | 按点击率排序的前 10 条，低样本异常记录不参与排行 |

### 6.5 图表说明

#### Banner 效果排行

- 类型：横向柱状图；
- X 轴：点击 UV 或点击率；
- Y 轴：Banner 标题；
- 展示：当前日期、客户端和位置下的 Top10；
- 目的：识别高点击量和高点击效率资源位。

### 6.6 明细表说明

字段：日期、Banner 位置、Banner 标题、曝光 UV、点击 UV、点击率。

排序规则：排行按指标降序；明细保留原始记录并分页。异常记录保留在明细表中，但不参与排行。

### 6.7 数据来源

当前读取：

`dashboard-v2/data/banner_click_20260704_20260804_all_clients.json`

接口：`bannerClickData`。

字段映射：`date`、`clienttype`、`position_id`、`title`、`uv_expose_count`、`uv_click_count`、`ctr_uv`。

### 6.8 当前限制

- 当前接口没有稳定返回 Banner 跳转播放 UV、有效播放 UV；
- 不能用点击 UV 代替跳转播放 UV；
- 客户端筛选当前只提供安卓和 M 站；
- 低曝光样本可能导致点击率极端值，因此点击率排行有最低曝光门槛/异常排除逻辑。

## 四、数据资产边界

## 当前有效数据

以下是当前页面实际读取或通过 `DATA_PATHS` 绑定的数据资产：

- `new_people_video_daily_active.json`：核心 DAU、新增设备、播放率；
- `人均播放时长_近30天_按客户端.json`：人均播放时长；
- `avg_play_count_by_client.json`：人均播放次数；
- `站内播放排名Top30_20260706_20260804.json`：内容榜单；
- `热搜总榜_20260704_20260804.json`：总榜热搜；
- `新用户热搜_词频_20260704_20260804.json`：新用户热搜词；
- `搜索使用率_按客户端_20260706_20260804.json`：搜索使用率；
- `首帧播放UV整体转化率_20260706_20260804.json`：首帧播放转化；
- `播放5分钟UV整体转化率_20260706_20260804.json`：5 分钟播放转化；
- `频道运营分析_dramaConversion_android_rrsp_xb_20260705_20260804.json`：首页频道运营；
- `home_section_ops_20260705_20260804.json`：首页板块运营；
- `banner_click_20260704_20260804_all_clients.json`：Banner 点击分析；
- `剧种播放占比_全部端口_20260705_20260804.json`：内容榜单中的剧种播放占比辅助数据。

说明：部分文件名保留了首次生成时的日期，但文件内容会被增量脚本原地追加；判断是否最新必须读取文件中的实际日期字段，不能只看文件名。

## 历史数据

以下类型的数据可能仍存在于工作区，但不代表当前页面正在使用：

- `dashboard-v1-baseline`、`dashboard-v1-original-baseline` 下的旧页面；
- `dashboard-demo` 下的演示数据和旧版页面；
- 文件名明确为早期快照且未被 `DATA_PATHS` 或 `LOAD_PATH_OVERRIDES` 引用的 JSON；
- 旧版搜索、热播、Banner 或首页分析导出文件；
- 历史截图、验收报告、设计方案和数据字典。

判断原则：只有当前 `dashboard-v2/app.js` 的最终 `DATA_PATHS`、`LOAD_PATH_OVERRIDES` 和页面渲染函数实际引用的文件，才属于当前页面数据源。

## 废弃模块

以下内容不应被当作当前看板主 Tab：

- 弹窗数据；
- Quick BI 中存在但当前前端未接入的推荐组件数据；
- 猜你喜欢 PV 明细中尚未接通的字段；
- Banner 跳转播放、有效播放等当前接口没有稳定返回的字段；
- 旧版 Tab4 页面或历史版本的频道分析布局；
- 仅存在于设计文档、mock-data 或旧页面中的功能。

如果后续要接入弹窗数据，必须先确认 Quick BI 的真实组件查询接口、字段口径和组件粒度，不能用 `sectionData`、`customNavigationDetail` 或 `bannerClickData` 代替。

## 五、禁止事项

后续 AI 或开发人员优化本看板时，必须遵守：

1. 不根据文件名猜测数据范围，必须读取数据内容中的实际日期；
2. 不把历史页面、旧版 Tab 或演示 JSON 当成当前页面；
3. 不把未被当前 `dashboard-v2` 引用的 JSON 当成当前数据源；
4. 不把热搜词榜误称为剧集热搜榜；
5. 不把播放 VV、点击 UV 或其他指标推算成播放 UV；
6. 不把 `dramaConversion` 的首页转化字段直接当成 Quick BI 搜索整体数据字段；
7. 不把缺失值、零值、无对比和负零混为同一种状态；
8. 不在缺少真实接口数据时手工编造、复制相邻日期或跨客户端填充；
9. 不删除异常明细；异常数据应保留并标记，排行可以排除；
10. 不改变原始排名、原始字段含义或接口口径来迎合图表展示；
11. 环比筛选必须基于完整原始数据重新计算，不能在上一次筛选结果上继续破坏性过滤；
12. 榜单类型、榜单状态和环比方向应避免无提示地叠加，避免用户点击后得到空表；
13. 新入榜必须通过当前日期与前一日内容集合比较判断，不能只依赖可能为空的原始状态字段；
14. 新增筛选器、指标或图表前，必须说明它影响的页面区域、数据字段和缺失数据行为；
15. 任何优化建议都必须明确区分“当前已有能力”和“建议新增能力”。

