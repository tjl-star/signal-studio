# AI 运营数据看板 Agent 规范

## 1. 角色与任务

你是本项目的产品、数据与开发协作 Agent，负责为影视视频 App 运营团队建设一个可运行的 AI 运营数据看板。

首要目标不是一次性开发完整企业 BI，而是先完成以下最小闭环：

```text
数据获取 → 数据校验 → 指标标准化 → Dashboard 展示 → AI 分析
```

第一阶段的 AI Dashboard 是 Quick BI 的增强层，不替代 Quick BI，也不开发用户管理、审批或复杂权限系统。

## 2. 已知业务背景

运营团队日常需要关注：

- 用户活跃与新增
- 用户留存
- 内容播放
- 搜索行为
- 首页及频道转化
- Banner、弹窗、猜你喜欢等推荐位效果
- 内容转化漏斗

当前痛点：

- 数据来源多、指标分散
- 人工日报效率低
- 指标口径可能不一致
- 异常原因依赖人工判断
- 缺少自动化趋势、异常和运营建议

## 3. 数据源事实与边界

### 3.1 当前已验证的数据源

公司已有 Quick BI 平台，包含用户、播放、页面、功能和内容等数据模块。

当前已验证的访问方式是：

1. 使用用户当前已登录且已授权的 Quick BI 会话。
2. 在看板页面设置筛选条件。
3. 读取页面结果或使用看板自身的导出功能。

### 3.2 尚未验证的能力

目前没有证据证明项目已获得以下能力：

- Quick BI 后台数据库直连
- 稳定、正式的数据 API
- 可用于无人值守任务的身份认证
- 可绕过看板审批或权限的数据抽取能力

因此，不得把浏览器查询或看板导出描述为“后台接口直连”。

### 3.3 数据权限

- 只能使用当前账号已经拥有的查看与导出权限。
- 不得绕过审批、行列权限、脱敏、水印或导出限制。
- 导出前确认数据范围和用途，避免不必要的全量明细。
- 不输出令牌、Cookie、连接字符串、密码或其他认证信息。
- 未确认自动化授权前，不建立定时无人值守的数据抽取任务。

### 3.4 可替换数据层

由于真实接口暂未确定，所有业务代码必须通过统一的数据适配层访问数据。

建议接口：

```python
class DataProvider:
    def get_dashboard_metrics(self, start_date, end_date): ...
    def get_content_ranking(self, start_date, end_date, segment=None): ...
    def get_funnel_metrics(self, start_date, end_date, funnel_type=None): ...
```

实现可以逐步替换：

```text
MockJsonProvider
    ↓
QuickBIExportProvider
    ↓
正式 API / 数据库 Provider
```

前端、指标计算和 AI 分析不得依赖某个数据源的私有字段。

### 3.5 强制数据源索引访问协议

凡是涉及“拿数据、查数据、搜索数据、导出数据、核对数据、分析已有数据”或需要调用数据接口的任务，必须先读取项目根目录的 `DATA_SOURCE_INDEX.md` 一次，再执行任何数据查询或数据口径判断。该要求适用于本项目的所有线程、子任务和后续会话。

读取索引后必须：

1. 根据索引选择唯一的主数据源；需要同时使用 `data_provider skill` 和 `quickbi-data MCP` 时，必须分别记录每个字段的来源。
2. 核对目标日期、日期范围、客户端/平台、用户类型、频道/页面、UV/PV粒度和去重口径。
3. 只使用索引中已登记且实际可调用的接口或组件；未连接、未验证或缺失字段必须标记为“待接入”，不得用相近字段替代。
4. 不得把 `dramaConversion`、`searchClickConversion`、`coreData` 等接口返回值拼接成 Quick BI 专属页面口径；除非索引和原始字段明确证明粒度、筛选条件、日期和口径一致。
5. 最终回答必须说明数据源、接口/组件、原始字段、日期和关键筛选条件；若索引与当前可用工具不一致，先报告阻塞原因，不得猜测或伪造结果。

如果 `DATA_SOURCE_INDEX.md` 不存在、无法读取或内容可能过期，必须先提示并更新/核验索引，再继续数据任务。普通代码、文案或与数据无关的任务不触发本协议。

## 4. 产品目标

### Phase 1：可运行 Dashboard Demo

- 使用 Mock JSON 跑通数据链路
- 建立统一指标模型
- 展示核心指标、趋势、排行和漏斗
- 支持动态刷新
- 预留真实数据和 AI 分析接口

### Phase 2：真实数据接入

- 确认数据源、认证方式、字段和权限
- 用小样本进行只读验证
- 建立字段映射和数据质量校验
- 将 Mock Provider 替换为真实 Provider
- 对照 Quick BI 核验关键指标

### Phase 3：AI 运营分析

- 生成每日摘要
- 检测异常
- 给出有数据依据的原因假设
- 输出可执行的运营建议
- 明确区分事实、推断和建议

### Phase 4：自动化

在数据接口、权限和口径稳定后，再考虑每日定时任务：

```text
获取昨日数据
→ 校验数据
→ 计算指标
→ 检测异常
→ 生成报告
→ 发布到获批渠道
```

## 5. 技术方案

### 前端

- HTML、CSS、JavaScript
- ECharts
- 支持指标卡、折线图、饼图、表格和漏斗图
- 页面必须适配常见桌面宽度
- 空数据、加载中、失败和数据过期必须有明确状态

### 后端

- Python
- FastAPI
- 负责数据适配、指标计算、数据校验、Dashboard API 和 AI 分析 API

### 存储

第一阶段使用 JSON 和 SQLite，优先验证数据链路。

数据量或并发量增长后，再评估 PostgreSQL 或 ClickHouse；不得提前引入不必要的基础设施。

### 分层

```text
Quick BI / 文件 / API / 数据库
              ↓
         Data Provider
              ↓
     数据校验与标准化层
              ↓
         Metric Engine
          ↙         ↘
 Dashboard API     AI Analysis
```

## 6. 标准数据模型

统一输出结构示例：

```json
{
  "date": "2026-07-29",
  "source": "mock",
  "generated_at": "2026-07-29T10:00:00+08:00",
  "user_metrics": {
    "dau": 1600000,
    "new_users": 50000,
    "retention_1d": 0.45,
    "retention_3d": 0.35
  },
  "play_metrics": {
    "play_uv": 1200000,
    "play_rate": 0.75,
    "avg_play_time_seconds": 3600,
    "avg_play_count": 5
  }
}
```

每个数据响应应尽量包含：

- 数据日期
- 数据来源
- 生成时间
- 指标口径版本
- 数据是否完整
- 校验状态

## 7. 核心指标

任何指标进入产品前，必须明确名称、定义、公式、单位、粒度、去重规则、时间范围和数据来源。

### DAU

每日活跃用户数。支持 iOS、Android、M 端拆分。

### 新增用户

当日新增用户数，并展示环比与趋势。

### 留存

至少包含次日留存和三日留存。必须明确按账号还是设备去重。

### 播放率

```text
播放用户数 ÷ DAU
```

### 人均播放次数

产生播放的用户的人均播放次数。必须明确分母是否为播放用户。

### 人均播放时长

产生播放的用户的人均累计播放时长，统一以秒存储，展示层转换单位。

## 8. 页面结构

### 页面 1：运营驾驶舱

- DAU
- 新增用户
- 播放率
- 人均播放时长
- 留存
- 近 30 日趋势
- 端分布

### 页面 2：内容运营分析

- 热播 TOP30
- 热搜 TOP30
- 剧种占比
- 新老用户分组
- 新入榜标识

### 页面 3：用户转化分析

- 搜索漏斗
- 首页频道漏斗
- 首页板块漏斗

有效播放的暂定定义为播放超过 5 分钟，但在真实接入前必须由业务方确认。

### 页面 4：推荐效果分析

- Banner
- 弹窗
- 猜你喜欢
- 曝光、点击、播放、有效播放及转化率

## 9. AI 分析规范

输入为标准化 Dashboard 数据，输出至少包含：

- 今日总结
- 显著变化
- 异常发现
- 可能原因
- 运营建议

规则：

- 所有结论必须引用具体指标、时间范围和对比基准。
- 不把相关性写成因果关系。
- 数据不足时明确说明无法判断。
- 原因分析应标记为“推断”或“待验证假设”。
- 建议应具体、可执行，并说明预期验证指标。
- 不生成无数据依据的泛化运营话术。

## 10. 数据质量要求

真实数据接入时，至少检查：

- 日期、时区和统计周期
- 字段类型与单位
- 空值和重复值
- 分页与导出上限
- 当前数据是否完整
- 指标口径版本
- 明细汇总与看板总数是否一致
- 同比、环比的基准日期是否正确

关键指标必须与 Quick BI 对照验证。出现差异时先停止扩展开发，记录差异、样本、口径和可能原因。

## 11. 开发原则

1. 先验证数据链路，再扩展页面。
2. 数据层、指标层、展示层和 AI 分析层必须解耦。
3. Mock 数据与真实数据使用同一份模型和接口。
4. 不猜测生产接口、请求参数、认证格式或指标口径。
5. 默认执行最小、只读、可验证的数据操作。
6. 不修改 Quick BI 配置、权限或数据源，除非用户明确授权。
7. 保留原始导出文件，清洗结果写入新文件。
8. 所有代码改动都应配套最小必要测试。
9. 不一次性开发完整企业系统。
10. 页面设计服务于运营判断，不为图表数量而增加图表。

## 12. Agent 工作流程

每次接到任务时：

1. 明确目标指标、日期范围、维度、输出格式和数据权限。
2. 检查工作区现有代码、配置和数据文件。
3. 如果使用真实数据，先做最小只读连通性与样本验证。
4. 报告数据来源和访问方式，不夸大连接能力。
5. 完成实现或查询后，验证结果数量、日期和关键字段。
6. 若涉及导出，确认文件实际生成后再报告成功。
7. 若存在口径或权限不确定性，明确列为未满足前提。

## 13. 第一里程碑验收标准

交付一个可本地运行的 AI 运营 Dashboard Demo：

- FastAPI 后端可启动
- 前端使用 ECharts
- Mock JSON 可驱动全部核心页面
- 数据适配层可替换
- 页面支持动态刷新
- 核心指标有明确口径
- 包含加载、空数据和错误状态
- AI 分析模块有稳定输入输出契约
- README 提供运行和验证方法

在第一里程碑完成前，不扩展复杂权限、企业级部署或未经验证的自动化数据抽取。

## 14. 强制数据日期与口径一致性规则

以下规则为本项目发布硬约束。违反任一条，不得将页面标记为数据已接入或数据正确。

### 14.1 日期必须逐字段一致

- 每次数据快照必须同时记录 `snapshot_date`、`data_date`、`generated_at`、时区、数据源和查询参数。
- 页面标题、日期筛选器、指标卡、趋势图、榜单和明细表，必须明确使用同一个目标 `data_date`；不同日期的数据必须分组展示并显式标注。
- “昨日”必须按运行时当地日期计算为 `date.today() - 1`，不得把旧日期硬编码为昨日。
- 全局 `dashboard_date` 不能覆盖或推断字段真实日期。每个指标卡、趋势点和榜单记录都必须保留自己的 `data_date`。
- 趋势最后一个数据点的日期必须与当前指标卡目标日期一致；日期选择器必须实际驱动查询或过滤，不能只改变页面文字。
- 快照不是目标日期时，必须显示“数据截至 YYYY-MM-DD”或“待刷新”，不得冒充昨日数据。

### 14.2 禁止混用旧数据和新快照

- 同一页面不得混用不同日期的 DAU、新增用户、留存、播放、搜索或转化数据，除非每组数据都明确标注日期和口径。
- 目标日期缺少数据时，显示“待接入”或“数据缺失”；不得保留旧日期的数值并将其状态改成已确认。
- 只有完成目标日期的原始数据查询、字段核验和口径核对后，字段才能标记为 `confirmed`；旧值必须标记为 `stale` 或 `pending`。
- 发布前必须逐项核对页面日期、字段日期、原始响应日期和 Quick BI 日期；任何不一致都必须阻断发布并记录差异。

### 14.3 用户、设备和榜单口径不可替代

- `dau` 只能使用账号/用户 DAU；`device_dau` 不得替代账号 DAU。
- 新用户、老用户和新增用户必须使用对应用户类型口径；新设备榜、老设备榜和设备数据不得替代用户榜。
- 总榜不得替代新用户榜或新增用户榜；接口只返回 Top10 时必须保留 Top10 并说明接口限制，不得伪造 Top30。
- 不得使用总榜比例、设备比例或其他指标推算缺失的用户指标、播放 UV、搜索 UV 或转化 UV。

### 14.4 缺失能力必须如实暴露

- 数据提供能力未返回的字段，统一显示“待接入”，不得用 mock 值、旧值、总榜值或相近指标填充。
- 每个字段必须登记接口名称、请求参数、响应字段名、数据日期、维度、单位和状态（`confirmed`/`pending`/`stale`）。
- 若账号 DAU、新增用户、留存等指标没有可验证接口，必须明确记录“不支持/待开发”，不得标记为真实当前数据。
- 数据源异常、日期过期或字段缺失时，页面应优先失败并提示原因，而不是静默展示看似完整的数字。

### 14.5 发布前强制校验

发布任何看板页面前，必须完成以下检查：

1. 解析最终 JSON，确认每条记录包含目标日期和来源元数据。
2. 抽查页面日期与至少三个核心指标的字段日期，确认完全一致。
3. 将核心指标与原始 data-provider/Quick BI 响应逐项比对，不能只比对页面总数。
4. 检查是否存在旧值、`undefined`、`null`、无来源数值或错误的“已确认”状态。
5. 检查筛选器切换后数据日期、用户口径、榜单类型和排序规则仍然正确。
6. 任一检查失败，页面必须标记为“待核验/待接入”，不得继续扩展页面或对外发布。

### 14.6 凭证管理

- 禁止把 data-provider、Quick BI 或其他数据源的明文密码写入 `agent.md`、代码、JSON、README、日志或提交记录。
- `coreData` 的密码必须通过环境变量 `DATA_PROVIDER_CORE_PASSWORD` 注入；命令输出、接口响应和最终报告不得回显密码。
- 发现用户在对话中提供凭证时，仅用于当前授权查询；后续任务仍从安全环境变量读取，不从项目文档读取明文凭证。
